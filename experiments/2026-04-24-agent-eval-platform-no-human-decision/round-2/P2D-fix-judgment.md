本次是第 2 次 P2D-fix,按终止条件规则强制走框架内修改、不回 P1。P2D 的五条强否定措辞虽重,但方向一致——都在说 v4 的"轻量 + 自愿 + 协作者"组合缺乏结构性护栏。v5 保留 v4 方向,加五层守护栏:

SanityFloorGate · 草案态必须通过 50 条明暗样本 AUC ≥ 0.65,UI 三明治警告 + 90 天强制决策
UpgradeTriggerEngine · 14 天 ≥ 100 runs / 挂 CI blocking / 进管理层仪表盘 / 承担 go/no-go 任一触发即强制入升级队列,超期降级为 warning-only
Held-out 集 + RewardHackingDetector · iteration_set / holdout_set 强制拆分,suggested_fix 只用 iteration 生成、升级只在 holdout 验证,"只在可见集变好"自动 flag
Measurement Reviewer 升为 P0 · 维度/判据/红线入库必须 MR 签字,PM 不能单方面拍板
LifecycleGovernor · 60 天无更新冻结 / 两周无人看 CI 降权 / 季度清理
核心立场不变:默认轻量 + judge 协作。但"轻量不等于无底线,协作不等于合谋"。