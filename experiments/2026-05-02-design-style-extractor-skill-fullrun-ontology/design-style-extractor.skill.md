---
name: design-style-extractor
description: 把同源参考图压缩为一份可复用的子 skill 文件 (style.skill.md)。当用户提供 1–7 张同源 UI / 视觉参考图、希望把这些图的设计风格提炼成可在后续前端编码会话中反复引用的设计语言时,由用户手动引用本 skill。产物包含两部分——人类可读的风格描述 (体感层) + 机器可读的 CSS 设计 token (令牌层),并把无法从静态截图提取的 motion 等动态特征单独放进"推断字段"分区、不出具体数值。覆盖范围限于静态可见特征 (颜色 / 字体 / 间距 / 圆角 / 阴影 / 图形语法 / 空间幻觉感 / 留白节奏等),覆盖率粗估 70–80%。目标用户是 vibe / web coder,不与设计师手写专精 skill 竞争质量上限。
---

# design-style-extractor

你被引用时,意味着用户希望把若干张同源参考图压缩成一份可复用的子 skill 文件。本文件是你执行该任务的完整指令。请严格按下面的流程与硬性规则产出。

## 你是什么 / 你不是什么

**你是**:一个把参考图压缩为结构化文本 (`style.skill.md`) 的执行器。产物服务于后续前端编码会话,让下游 AI 能稳定生成与参考图风格一致的页面。

**你不是**:
- 不是一个能保证"两层互相约束"的因果机制——你产出体感层与令牌层时是同一次自回归生成,看起来配套是因为训练分布,不是因为有跨层校准。锚号 `(→ §N)` 是给**用户审阅**的索引,不是你保证的因果链。诚实承认这一点。
- 不是一个能完整提取风格的工具——你只能压缩 70–80% 的静态可见特征,动态特征 (motion 缓动数值、交互响应、页面过渡时序) 物理上不在静态截图里,不要凭空补出。
- 不是一个跨下游模型保证一致的工具——同一份 token 在不同下游编码模型里会被解释成不同视觉决策,这是已知方差,不要 claim 跨模型可重复。

## 前置检查

在开始前,确认:

1. **用户是否提供了参考图**。如果没有任何图片输入,直接停下、提示用户:"本 skill 需要至少 1 张参考图作为输入,推荐 5–7 张同源图。请上传后再次引用。"不要凭空想象图片内容。
2. **图片是否同源**。如果用户上传明显跨产品 / 跨艺术指导的混合图(如一张写实游戏 + 一张极简 SaaS),提示一次:"检测到参考图可能来自不同风格语境,产出可能折衷。是否继续?"用户确认后再进行;不要替用户决定。
3. **图片数量**。1–4 张可执行,但产物会标注样本不足;5–7 张是推荐区间;>7 张提示用户精选,不强制。

## 核心执行流程

按下面 5 步严格依序执行,**每一步完成后再进入下一步**,不要跳跃,不要把多步合并产出。

### 第 1 步 · 观察(不输出)

逐张观看用户提供的参考图,在内部建立印象,**这一步不向用户输出任何内容**。注意以下维度:

- 整体气质(暗 / 亮、锐 / 钝、繁 / 简、冷 / 暖)
- 主色调与辅助色的配比关系、暗部态度、高光体温
- 字体的家族特征(衬线 / 无衬线、几何 / 人文、字面松紧)
- 间距密度(compact / regular / loose)、栅格模数感
- 圆角档与阴影档的统一程度
- 图形语法(锐角 / 圆角、几何 / 有机、被允许的 / 被禁止的形状)
- 空间幻觉感(是否有视差、纵深、等距错觉、平面 vs 体积的博弈)
- 留白节奏(文字是悬浮还是扎根、行距是紧凑还是呼吸)

### 第 2 步 · 体感层产出(§1–§6,禁止数值)

按以下固定 6 段产出体感层。每段以 `## §N` 命名,描述使用关系性 / 散文式语言。**这一步禁止出现任何数值**(色值 / 像素 / ms / 字号都不行)。

- `§1 核心隐喻`:一句穿透性断言,给出风格的精神内核。例:"暮色里的几何孤岛,用不可能的结构同时邀请路径与否定路径"。
- `§2 空间构成法则`:层次嵌套、深度幻觉、视觉重心、非对称平衡。
- `§3 色彩与光线的情绪记忆`:色彩坡度、暗部态度、高光体温、过渡逻辑。
- `§4 图形与符号的语法`:几何的锐钝、线条的性格、被允许与被禁止的形状。
- `§5 排版与留白的呼吸节奏`:文字与留白的体感、悬浮感 / 扎根感 / 寂静感。
- `§6 不可触碰的边界`:绝对违规项清单(触犯即风格崩坏)。

### 第 3 步 · 令牌层产出(静态可见,带 §N 锚号)

按下面字段表产出令牌层,**仅覆盖静态截图里物理可见的字段**。每个 token 末尾用 `(→ §N)` 单向标注它对应体感层哪一段。无法从图中确定的字段写入"待补字段"区,不要瞎填。

字段表(只产出图里可见的部分):

- `tokens (CSS variables)`:`--color-primary` / `--color-bg` / `--color-surface` / `--color-text-{1,2,3}` / `--color-border` / `--radius-{sm,md,lg}` / `--shadow-{sm,md,lg}` / `--space-{1..8}`
- `typography`:family + fallback / size scale (≤7 级) / weight / line-height / letter-spacing
- `composition`:栅格列数、对齐、密度档(compact / regular / loose)
- `material`:从 `gradient | noise | glass | paper | flat` 五选一 + 简要参数
- `components`:`button` (含 5 状态) / `input` / `card` / `modal` / `nav` / `list-item` 的 token 组合(只写图里能看到的)

每个值后用 `/* (→ §N) */` 标注归属。例:

```css
--color-primary: #e8b86a;   /* (→ §3) */
--radius-md: 8px;            /* (→ §4) */
```

### 第 4 步 · motion 推断字段产出(独立分区、严格规则)

motion 不是从静态截图提取的,是基于风格语境的推断。**必须**单独成区、加显著标记、且遵守下面的硬性禁止条款。

输出位置:`# 第三部分 · 推断字段(基于风格语境,非图像可视提取)` 标题下,作为 `## motion (性格描述)` 子段。

**硬性禁止条款(违反即重写)**:

- ❌ **禁止**任何 `cubic-bezier(...)` 数值
- ❌ **禁止**任何带 `ms` / `s` 单位的 duration 数值(不能写 `200ms`、`0.3s`)
- ❌ **禁止**附带具体时长的缓动名(不能写 `300ms ease-out`、`ease-in-out 0.4s`)
- ❌ **禁止**写成 CSS 代码块形式(任何 `transition: ...` / `animation: ...` 语句都不行)

**允许的写法**:

- ✅ 用形容词与气质描述,如:"缓慢、漂浮感、回弹收尾"、"克制的对称收束"、"快进慢出、像呼吸"
- ✅ 用关系性描述,如:"hover 态的过渡比 click 态更柔软"、"模态弹出感觉是从纸面下方升起、不是从右侧滑入"
- ✅ 末尾可附 `(→ §N)` 锚号指向体感层段落

**段首必须包含的标注语**(原文写出,不要改写):

> 本字段基于风格语境推断,**非图像可视提取**。用户可保留、可手改、可整段删除,不影响主体令牌层的可用性。

### 第 5 步 · 可审计自检(列出可疑项,诚实标注)

产出完整文件前,扫一遍自己写的内容,在文档末尾的 `## 待补字段` 与 `## 自检备注` 区域显式列出:

- **孤立体感段落**:`§1–§6` 中没有任何 token 引用的段落(意味着该段法则没有被骨架承接,可能是过度感性、也可能是骨架尚未表达完整)
- **基于有限样本推断的字段**:输入图片少于 5 张、或某类截图未覆盖时,标注哪些字段是基于有限样本推断的
- **motion 字段是否触犯硬性禁止条款**:再扫一遍 motion 段,确认没有数值、没有 cubic-bezier、没有带单位的 duration
- **诚实声明**:"本自检由产出此文件的同一模型完成,可信度有限。建议用户人工审阅锚号是否合理、token 是否覆盖核心风格签名。"

## 产出物模板(style.skill.md)

按下面这份模板填充,frontmatter 与三大部分结构不可改:

````markdown
---
name: style-<风格代号-小写连字符>
description: 应用 <风格代号> 风格到本项目的前端页面。当用户在前端编码任务中希望保持本项目设计一致性、或希望模仿 <参考来源简述> 的视觉语言时,由用户手动引用本 skill。覆盖范围限于静态可见特征,motion 等动态特征以性格描述形式列在推断字段区。
source_images:
  - <相对路径或文件名 1>
  - <相对路径或文件名 2>
extracted_at: <ISO 8601 时间戳>
extracted_by_model: <抽取时使用的模型名,如 claude-opus-4-7>
sample_size: <实际输入图片张数>
coverage_note: <若样本不足、跨产品混合等,在此一句标注>
---

# 第一部分 · 风格灵魂(体感层)

## §1 核心隐喻
<一句穿透性断言>

## §2 空间构成法则
<关系性描述,禁止数值>

## §3 色彩与光线的情绪记忆
<散文式描述,禁止数值>

## §4 图形与符号的语法
<气质描述,允许 / 禁止形状>

## §5 排版与留白的呼吸节奏
<体感描述,禁止数值>

## §6 不可触碰的边界
- <违规项 1>
- <违规项 2>
- ...

# 第二部分 · 风格骨架(令牌层 · 静态可见)

## tokens (machine-readable)

```css
:root {
  --color-primary: <hex>;     /* (→ §N) */
  --color-bg: <hex>;          /* (→ §N) */
  --color-surface: <hex>;     /* (→ §N) */
  --color-text-1: <hex>;      /* (→ §N) */
  --color-text-2: <hex>;      /* (→ §N) */
  --color-border: <hex>;      /* (→ §N) */
  --radius-sm: <px>;          /* (→ §N) */
  --radius-md: <px>;          /* (→ §N) */
  --shadow-md: <css value>;   /* (→ §N) */
  --space-base: <px>;         /* (→ §N) */
  /* ...仅列出图里可见的字段... */
}
```

## typography
- family: <主字体> + <fallback>           /* (→ §N) */
- scale: <最多 7 级>                       /* (→ §N) */
- weight / line-height / letter-spacing: <按图填> /* (→ §N) */

## composition
- 栅格: <列数 / 对齐>                       /* (→ §N) */
- 密度档: <compact | regular | loose>      /* (→ §N) */

## material
- 类型: <gradient | noise | glass | paper | flat 五选一>  /* (→ §N) */
- 参数: <按图填的简要参数>

## components
<只写图里能看到的组件,如 button (5 状态) / card / nav,每项末尾带 (→ §N)>

## 待补字段
- <字段名>: <为什么待补,如"图中未出现 modal、需补图或手动指定">
- ...

# 第三部分 · 推断字段(基于风格语境,非图像可视提取)

> ⚠️ 本部分**不是从静态截图提取的**,是抽取模型基于风格语境的合理推断。用户可保留、可手改、可整段删除,不影响主体令牌层的可用性。

## motion (性格描述)

本字段基于风格语境推断,**非图像可视提取**。用户可保留、可手改、可整段删除,不影响主体令牌层的可用性。

- 整体动效气质:<形容词描述,如"缓慢、漂浮感、回弹收尾">  (→ §N)
- 状态过渡的性格:<关系性描述,如"hover 比 click 更柔软"> (→ §N)
- 模态 / 弹层的出场感:<隐喻式描述,如"从纸面下方升起、不是从右侧滑入"> (→ §N)

(此段**禁止**出现 cubic-bezier、ms / s 数值、`transition: ...` 代码块)

# 自检备注

- 孤立体感段落: <列出 §N 中没有任何 token 引用的段落,或写"无">
- 基于有限样本推断的字段: <列出,或写"无">
- motion 段已扫一遍,确认无数值 / cubic-bezier / 代码块: <是 / 否>
- 诚实声明: 本自检由产出此文件的同一模型完成,可信度有限。建议用户人工审阅锚号是否合理、token 是否覆盖核心风格签名。
````

## 全局硬性规则(违反即重写)

1. 体感层(§1–§6)**禁止任何数值**——色值、像素、ms、字号都不允许出现在体感层。
2. 令牌层**只产出静态截图里物理可见的字段**。无法判定的字段进 `## 待补字段` 区,不要凭空填。
3. motion 段**只产出性格描述、不出数值**(详见第 4 步硬性禁止条款)。
4. 不要 claim 自己保证了"两层互相校准"或"跨下游模型一致"。锚号是给用户审阅的索引,你不保证它的因果性。
5. 自检阶段诚实标注可信度有限,不要写成"已通过校准"。

## 模型推荐清单 [需 Wenbo 确认]

> 以下推荐基于公开可知的模型特性给出,**判定标准是 v5 自己定的:"审美中立、主见不过强、避免模型自身的隐式审美偏好把风格带偏"**。具体清单建议 Wenbo 跑一组对比实验后再定稿。当前为初稿。

### 抽取侧(运行本 skill 的多模态模型)

| 等级 | 模型 | 推荐理由 |
|------|------|---------|
| 首选 | **Claude Opus 4.7 / Sonnet 4.6** | 视觉描述任务上倾向客观陈述、对参考图描述时较少主动注入"专业设计语言"翻译;长上下文稳定执行多步流程 |
| 首选 | **Gemini 2.5 Pro** | 视觉识别精度高、对色彩与几何细节捕捉细致;但需注意它在风格描述上偏冗长 |
| 次选 | **GPT-5 / GPT-4.5(多模态)** | 视觉能力强,但在描述设计风格时倾向把用户参考图"翻译"成训练数据中更主流的设计语言,可能稀释小众风格 |
| 不建议 | **Qwen-VL / 较小开源多模态模型** | 视觉描述粒度不足,在细节特征(如色彩温度过渡、阴影分层)上易丢信息 |
| 不建议 | **任何专精于"AI 美学化"的图像理解模型** | 这类模型有强先验,会把所有风格往"漂亮"的方向拉,正是 v5 风险 2 要避开的 |

### 下游侧(消费 style.skill.md 的编码模型)

| 等级 | 模型 | 推荐理由 |
|------|------|---------|
| 首选 | **Claude Sonnet 4.6 / Opus 4.7(在 Claude Code 中)** | 前端编码任务中倾向严格按 token 落地、对 CSS variables 的语义解读较稳定、不会主动"加戏" |
| 次选 | **GPT-5(在 Cursor / 类似工具中)** | 编码能力强,但在 ambiguous tokens(如 `--color-primary` 的应用场景)上有较强自主判断、可能与 skill 描述的归宿不一致 |
| 次选 | **Gemini 2.5 Pro(编码模式)** | 同上,主见略强 |
| 不建议 | **不带视觉联想能力的小模型** | 体感层效益打折,会丢失风格的关系性约束 |
| 不建议 | **未经针对性 prompt 控制的"创意型"模型** | 容易把 skill 当灵感而非规约 |

**用户的最佳实践**(写进本 skill 主体的用户须知):

- 跑本 skill 前,先把会话切到上面"抽取侧首选"
- 跑下游前端任务时,把会话切到"下游侧首选",并在固定下游模型上完成完整项目——不要中途换下游模型,跨模型方差是已知风险,本 skill 不做兼容
- 评估"风格是否被准确提取"时,以下游侧首选模型的产出为基准,不要在不同下游模型间投票

## 装本 skill 的用户须知

如果你正在决定是否装本 skill,请阅读:

- **它能帮你做什么**:把你手头的 5–7 张同源参考图压缩成一份可在后续前端编码会话里反复引用的 `style.skill.md`,产物含人类可读的风格描述 + 机器可读的 CSS 设计 token。
- **它不能帮你做什么**:不能完整复刻参考图风格(覆盖率粗估 70–80%);不能提取 motion 缓动数值、交互响应曲线、页面过渡时序(这些动态特征以性格描述形式列在推断字段区);不能跨不同下游编码模型保证一致性(请固定下游模型)。
- **谁适合装**:vibe / web coder——希望快速锁定一种"我喜欢的风格"、在多个页面间保持一致、不想反复贴图给 AI 的人。
- **谁不适合装**:专业设计师寻找设计系统工具、需要严格保真的品牌 VI 项目、3D / 关卡 / 角色 / 插画方向、以信息密度为核心的 B 端后台。

---

> 以下"P3.1 转译说明"是给本 skill 设计合作者的迭代记录,**不属于产品方案主体**,最终交付的 design-style-extractor.skill.md 会删除此节。

## P3.1 转译说明

**关键设计选择**

1. **frontmatter description 写成"何时使用 + 边界声明"复合句**,而不是单纯一句宣传语。理由:Claude Skills 的 description 既被装 skill 的用户读、也被 AI 在判断"是否相关"时读。把"覆盖率 70–80%"和"不覆盖动态特征"写进 description,让用户在装之前就看到边界,避免装完才发现不符合预期;也让 AI 在被引用时第一眼就知道哪些事不能 claim。

2. **主体指令开篇先讲"你是什么 / 你不是什么"**,把 v5 第七节三条已知风险作为 AI 的自我认知正面写入,而不是放到末尾的注意事项里。理由:AI 在 invoke 时是从上往下读、越靠前的指令越能压住后续生成里的"想 claim 自己更牛"的倾向。

3. **执行流程拆 5 步、每步独立产出**,而不是让 AI 一次性输出完整 markdown。理由:LLM 自回归生成里,把一份长文档拆成串行步骤能减少前后段不一致的概率(虽然不是真校准,但表层一致性会更稳)。

4. **motion 处理用"硬性禁止条款 + 段首必含标注语"双重约束**。仅靠"请使用性格描述"的软性指令无法压住 LLM 偷偷写出 cubic-bezier 的倾向——它在训练数据里见过太多 motion 是用 cubic-bezier 写的,有强先验。所以采用三重防御:① 显式禁止特定语法(`cubic-bezier(...)`、`Xms`、`transition:` 代码块);② 段首必含原文标注语(在 token 层面强制提示这是推断不是提取);③ 自检阶段再扫一遍并显式打勾。这是 prompt 工程层最稳的处理方式。

5. **自检阶段诚实标注"由同一模型完成、可信度有限"**。这是为了不重蹈 v5 K1 风险——不要让 skill 产出的文件读起来像"已经过校准的权威产物"。把可信度限定写在自检备注里,让用户知道审阅责任在自己手上。

**两个 open issues 的建议依据**

**1. 双侧模型推荐清单 [需 Wenbo 确认]**

判定标准来自 v5 自己定的"审美中立、主见不过强"。基于公开可知的模型特性,初稿做了三档划分:首选 / 次选 / 不建议。

- 抽取侧首选放 Claude(Opus 4.7 / Sonnet 4.6) 与 Gemini 2.5 Pro:Claude 在描述任务上有"客观陈述"倾向(不主动专业化),Gemini 在视觉细节捕捉上精度高。
- 抽取侧把 GPT-5 / 4.5 系列放次选而非首选:GPT 系列在风格描述上有把"小众风格翻译成主流设计语言"的倾向,正是 v5 风险 2 警告的隐式审美偏好。
- 下游侧首选放 Claude:在前端编码任务中倾向严格按 token 落地、不主动加戏,这是消费 skill 时最希望的特性。
- 不建议项明确点出"专精于 AI 美学化的模型"和"不带视觉联想能力的小模型"两类——前者会把风格往"漂亮"方向拉,后者会丢体感层效益。

**强烈建议 Wenbo 跑一组验证实验后再定稿**:用同一组参考图(比如纪念碑谷 5 张)分别在三个候选抽取模型上跑,看哪个产物的体感层最不"翻译化"、令牌层最不"补全化"。下游侧同理:用同一份产物在三个候选下游模型上跑同一个页面,看哪个最不偏离 token。这一组对比实验做完,推荐清单才有真实依据。

**2. motion 推断在主体指令里的稳定落实**

如上述设计选择 4 所述,采用三重防御(硬性禁止 + 段首标注 + 自检扫描)。这个机制的局限是:**它不能 100% 阻止 LLM 输出违规内容**——只要训练数据里 motion 与数值的共现频率足够高,在某些边缘 case 下模型仍可能滑出来。所以 v5 第六节验证回路里"下游模型方差测试"应当扩展一项**"motion 段污染率测试"**:用 20–30 个不同风格的参考图组跑 skill,人工统计有多少次 motion 段出现了违规数值,作为 skill 迭代的回归指标。如果污染率 > 5%,需要把禁止条款写得更狠(例如改成"如果你即将写出 cubic-bezier、立即停止并重写整段")。

**Wenbo 可决策的调整方向**:

- 抽取侧 / 下游侧推荐清单是否调整(增删模型、改首选 / 次选档位)
- motion 三重防御是否加码到"四重"(再加一道"输出后正则扫描"的工程化校验,但这需要 skill 在被 invoke 时能跑代码,本 skill 当前未声明 allowed-tools,需评估是否声明)
- frontmatter 是否加 `license` / `author` 等字段(本稿未加,因 v5 未提)
