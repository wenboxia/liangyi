# idea-v4.md · 设计风格提炼 Skill

## 一、产品定位

**一个把参考图压缩为"可移植、可复用、可审计"的设计风格 skill 文件的元 skill**——用户在自己的会话里手动引用本 skill，喂入若干张同源参考图，它产出**另一份具备完整 skill frontmatter 的子 skill 文件**，用户后续在前端编码任务里再次手动引用这份子 skill，下游 AI 就能稳定生成与参考图风格一致的界面。

它的产物**不是**一次性界面，**不是**临时提示词，**不是**针对单一风格手写的 skill 集合，而是一个能反复跑、跑出不同风格 skill 的"风格提炼器"。

**双轨服务对象**：本 skill 对两类用户都成立——对普通 vibe / web coder，产物是开箱即用的成品资产，引用即可让下游 AI 稳定出风格；对专业设计师，产物是一份高质量初稿，可在其上继续精修出更专精的风格 skill。底色是 vibe coder 友好（低门槛、随手可跑），但不排斥专业用户把它当快速草稿工具。

**本 skill 自己长什么样**：它就是一份标准的 `design-style-extractor.skill.md` 文件——一个带 frontmatter 的 markdown 指令文件，用户拿到手后放进 skill 库，在会话中显式引用即可启动。它的 frontmatter 形如：

```yaml
---
name: design-style-extractor
description: 把若干张同源参考图压缩为一份可复用的 style.skill.md（含设计 token 与风格体感描述）。当用户希望把参考图风格提炼成可在后续会话中复用的设计语言时，由用户手动引用本 skill。
---
```

**关于触发方式**：本 skill 与它产出的子 skill 都由用户手动引用调用，不依赖系统自动触发。description 字段写得好仍有意义（帮助用户在自己的 skill 库里检索定位），但本方案不假装是一个能被 marketplace 系统自动分发的触发型 skill。

## 二、核心机制（输入 → 处理 → 输出）

### 2.1 输入侧

- **图片数量**：**推荐 5–7 张**。低于 5 张依然可以跑，skill 会执行，但产物会显式标注"输入样本不足，风格变异边界未覆盖"，让用户自行判断是否补图。极端情况下 1 张也能跑——只是产物的可复现性与覆盖度递减。
- **图片类型推荐覆盖**（不是必须）：
  1. 典型主界面（风格的核心面貌）
  2. 细节或材质特写（按钮 / 卡片 / 图标 / 纹理，承载微观语法）
  3. 状态或边缘场景（hover / 选中 / 弹层 / 空状态 / 极限光照，揭示风格的韧性与破裂点）

  缺类时 skill **不报错、不阻塞**，只在产物里把对应章节的字段标记为"基于有限样本推断"。
- **建议（非强制）**：同一产品同一版本；尽量避免跨产品混搭、拼贴图、带浏览器 chrome 或手机壳的截图；分辨率长边越高越好。允许附一句风格上下文（如"参考《纪念碑谷》的关卡过场"）作为辅助语境。

**门槛设计原则**：以推荐替代强制。推荐项标明"为什么推荐"（5–7 张能覆盖风格变异、分类截图能让骨架更准），让用户在质量与便利之间自己取舍——而不是用硬门槛把样本不齐全的用户拒之门外。

### 2.2 处理侧

本 skill 是一份 markdown 指令文件，跑在**用户当前会话的当前模型**上——它**没有任何切换模型的能力**。

但模型选择对效果至关重要，因此本 skill 会在文档里给出**模型推荐清单**（首选 / 次选 / 不建议），由用户在跑 skill **之前**自行把会话切换到推荐模型。推荐清单是讨论阶段需要决策的内容（市面上哪些多模态模型在视觉风格识别上最强），最终会作为本 skill 文档的一部分附上。

**双层提取的产品哲学（两层互为约束）**：

- **体感层**（关系性语言）：穿透表象，命名风格的精神内核与组织逻辑——它约束令牌层不退化为冷冰冰的色卡复制。
- **令牌层**（结构化字段）：把可量化的视觉决策固化为机器可读 token——它约束体感层不飘成无法落地的散文。

两层互为约束是这个产品哲学的支点。该约束在执行层面通过下面 4 步流程 + 单向引用机制落地：

1. **观察阶段**：模型逐张观看用户提供的参考图，不输出任何内容，只在内部建立印象。
2. **体感层产出**：按固定 6 段结构产出体感层内容，每段以章节编号 §1–§6 命名（核心隐喻 / 空间构成 / 色彩光线 / 图形语法 / 排版留白 / 不可触碰边界）。这一步禁止出现任何数值。
3. **令牌层产出**：按固定字段表产出令牌层内容（颜色 / 字体 / 间距 / 圆角 / 阴影 / 组件 / motion / composition / material）。**每个 token 末尾用 `(→ §N)` 单向标注它服务于体感层哪一段**——单向标注比双向引用更不易错乱，但承载的语义仍是双层耦合：每个工程化决策都必须能溯源到一个体感法则。
4. **自检阶段**：模型扫一遍完整产物，列出体感层中没有任何令牌引用的段落（提示用户该段可能纯感性、未被骨架承接，或骨架尚未表达完整），以及令牌层中标注为"基于有限样本推断"的字段。这一步是双层互相约束的可见化检查。

### 2.3 输出侧

产物是一份 **`style.skill.md`**，**带完整 skill frontmatter**，可被用户在后续会话中作为子 skill 引用。结构如下：

```markdown
---
name: style-<风格代号>
description: 应用 <风格代号> 风格到本项目的前端页面。当用户在前端编码任务中希望保持本项目设计一致性、或希望模仿 <参考来源> 的视觉语言时，由用户手动引用本 skill。
source_images:           # 字符串数组，本地相对路径或文件名，仅作为元数据记录，不影响下游执行
  - imgs/ref-01.png
  - imgs/ref-02.png
extracted_at: 2026-05-02T14:30:00Z   # ISO 8601 时间戳
---

# 第一部分 · 风格灵魂（体感层）

## §1 核心隐喻
[一句穿透性断言，给出风格的精神内核]

## §2 空间构成法则
[关系性描述：层次嵌套、深度幻觉、视觉重心、非对称平衡]

## §3 色彩与光线的情绪记忆
[散文式描述色彩坡度、暗部态度、高光体温]

## §4 图形与符号的语法
[几何的锐钝、线条的性格、被允许与被禁止的形状]

## §5 排版与留白的呼吸节奏
[文字与留白的体感]

## §6 不可触碰的边界
[绝对违规项清单]

# 第二部分 · 风格骨架（令牌层）

## tokens（machine-readable）
:root {
  --color-primary: #e8b86a;   /* (→ §3) */
  --color-bg: #1a1622;        /* (→ §3) */
  --radius-md: 8px;            /* (→ §4) */
  ...
}

## components / typography / motion / composition / material
[按字段填充，每项末尾带 (→ §N) 单向锚]

## 待补字段
[列出无法从图中确定的字段，供用户审阅、补图或手动指定]
```

**`source_images` 与 `extracted_at` 的格式与作用**：前者是字符串数组，写本地相对路径或纯文件名即可（不要 Base64、不要远程 URL，避免膨胀）；后者是 ISO 8601 时间戳。两者都只是元数据记录，下游模型读取这份子 skill 时把它们当作普通文本注释处理——纯文本模型遇到这两行不会报错也不会去尝试加载图片，因为本 skill 不要求下游具备图像加载能力。如果用户担心元数据干扰特别严格的下游，可以在分发前手动删除这两行，不影响子 skill 主体的可用性。

引用规则在执行上是**单向**（令牌层 token → 体感层段落锚 `(→ §N)`），目的是让模型在单次生成中只需维持一个方向的对应关系而不错乱；但语义上承载的仍是双层耦合——每个 token 必须有体感层的归宿，体感层中孤立段落会被自检阶段显式标出。这是把"互相校准"工程化、可执行化，而不是放弃它。

去掉了过去版本里的 `confidence: { color: 0.x }` 字段——这种分数是 LLM 凭空写出来的，没有统计基础。"待补字段" 区块也明确定位为给用户审阅的提示清单，不承诺下游模型一定会读它。

## 三、形态选择：万能抽取器，产出资产，且承认在专精用户面前的质量天花板

如第一节所述，本方案的形态就是"万能抽取器"——一个 skill，跑一次产出一份风格 skill，**不**做手写具体风格库（纪念碑谷.skill.md / 锈湖.skill.md 这种逐个手写的形态）。

**产物的自我定位是"资产"，但承认在专精场景下的天花板**：本 skill 产出的 `style.skill.md` 对绝大多数 vibe / web coder 而言就是开箱即用的成品资产——拿来即可注入下游编码会话、稳定出风格、跨页面复用。同时诚实承认：一个真懂纪念碑谷美学的人手写的 skill，几乎一定胜过本 skill 自动抽取出来的版本——因为人手写时融入了"哪些是核心、哪些是噪音、哪些允许变异、哪些是不能动的风格签名"这种判断。所以对专业设计师而言，同一份产物可以被当成一份高质量初稿，在它之上继续精修。资产 vs 初稿不是降级关系，而是同一份文件在不同用户层级下的不同用法。

那为什么仍然选万能抽取器、不做手写专精库？因为它和手写专精解决的根本不是同一类问题：

1. **覆盖**：手写库永远滞后——用户想要的下一个风格库里大概率没有；万能抽取器当场能跑。
2. **杠杆**：万能抽取器跑一次就产出一份手写专精形态的文件——对专业用户它是精修起点，对普通用户它直接就是终点。
3. **维护**：手写库随参考产品迭代会僵化；万能抽取器重新喂新图就能更新。
4. **个性化**：用户的参考图组合往往是自己的私人审美剪贴板，手写库里不可能有现成的。

## 四、与"直接喂图给 GPT Image / Gemini 多模态"的差异

本 skill 的不可替代价值在四点——核心主张是**把风格固化成可工程化的资产**：

1. **可移植**：style.skill.md 是纯文本，可注入任何 System Prompt、任何后续会话。下游消费时——令牌层（颜色、字号、间距、组件 token）在**任何**模型上都有效，包括纯文本 LLM；体感层（隐喻、关系性描述）在带视觉联想能力的下游模型上发挥最大价值，在弱视觉模型上**有损但不归零**——比喻语言依然能给出方向，只是损失一部分张力。卖点是"令牌层全场景有效 + 体感层强场景增益"，而不是"两层在所有下游都满血"。
2. **可重复**：同一份 skill 跑 100 次 coding，token 与体感坐标都是同一份，结果可比对、可 diff。直接喂图每次输出都不一样。
3. **可审计 / 可手改**：用户能 diff 两份 skill、能把 `--color-primary` 改掉、能在体感层把"暮光橙灰"改成"晨雾蓝白"。图片改不动。可改性是资产可演化、可个性化的核心特性，不是因为产物是半成品。
4. **可缓存 / 可组合**：skill 一次抽取、N 次复用；CSS variables 直接进 Tailwind config / theme / Storybook。每次重新喂图的方案在大型项目里成本不收敛。

一句话：**直接喂图是一次性即兴；本 skill 是把风格固化成可工程化的资产**。

## 五、适用场景边界

**适用**：

- 模仿既有产品的 2D UI 视觉风格
- 给 vibe coding 项目锁定设计语言、跨多个页面保持一致性
- 有强烈视觉立场、可被压缩为一种"美学主张"的风格化产品——艺术游戏、叙事交互页面、独立品牌站、氛围型数字体验
- 普通 vibe / web coder 直接复用产物作为成品风格资产
- 专业设计师把产物作为快速草稿，再精修出专精风格 skill

**不适用 / 效果递减**：

- 3D 场景美术、关卡设计、角色设计、插画生成、品牌 VI 重塑
- 以信息密度和操作效率为核心的 B 端后台
- 用户其实只想要"好看"而没有具体参照物——本 skill 不负责生成审美主张
- 下游 AI 完全不具备视觉联想能力时，体感层效益打折（但令牌层仍然有效；`source_images` 等元数据字段不会引发报错）
- 对风格保真度有极致要求的场景——此时建议在本 skill 产物之上人工精修，或找懂这个风格的人手写专精 skill

## 六、验证回路

第一版必然不完美。验证逻辑分两层：

**作者自验（早期闭环）**：

1. **抽取**：拿一组参考图（如纪念碑谷过场截图 5–7 张）跑 skill，得到 `style-monument-valley.skill.md`
2. **复现**：把这份 skill 喂给下游 AI（Claude Code 或 vibe coding 工具），让它生成一个全新页面（登录页 / 列表页 / 设置页）
3. **对照三问**（每问都给可执行判定）：
   - **风格识别度**：陌生人单看生成结果，能不能说出"这像纪念碑谷"？判定方式见下文外部盲测。
   - **非机械复制 vs 风格跑偏**：通过两条可执行规则同时判定——
     - **非机械复制**：生成页面的布局结构、文案、组件组合不能与任何一张原参考图构成"换皮级复刻"，需是新页面但带同种风格语言。
     - **未跑偏**：用 skill 中令牌层 8 项核心 token（主色 / 背景色 / 字体家族 / 行高档 / 圆角档 / 阴影档 / 栅格列数 / material 类型）作为基准，生成产物中实际呈现的对应视觉决策 ≥7 项与 skill 一致即视为风格未跑偏。两条同时满足才算通过。
   - **可改性**：手动把 skill 里某个 token 改值（如 `--color-primary` 改色相），下游产物对应位置颜色随之变化，且变化方向可预测（不是改主色却变了别处），即视为通过。
4. **回归用例**：维护 3–5 套固定参考图集（写实 / 低多边形 / 极简 / 暗黑解谜 / 田园），每次 skill 迭代跑一遍同一组用例，对比前后产物。

**外部验证（公开发布前的准入门槛）**：

作者自审是早期开发的必要环节，但不足以证明 skill 真的 work。在公开发布前，至少需要：

- **盲测样本**：召集 3–5 位非作者背景的设计 / 前端从业者，让他们看生成结果（不告知是哪个风格的提炼），独立写出"这像什么风格"。命中率作为风格识别度的客观指标。
- **基线对照**：同一组参考图，分别用"直接喂图"和"本 skill"两条路径生成同一个页面，让盲测样本投票哪条路径产物更接近原风格。**这条仅作为下限基线**——本 skill 必须在多数场景里不输于直接喂图，否则直接证伪。
- **失败信号公开**：如果某类风格（例如手绘 / 高度叙事性的 motion）盲测明显不及格，在 skill 文档里明确标注它不适用，而不是隐藏。

**成功标准是绝对线，不是相对线**：本 skill 要做到的是"用户拿着 skill.md 而不是图片，下游 AI 也能稳定做出'对的味道'"——这是终极目标，做不到 skill 就没价值，做到了才是真资产。"不输于直接喂图"只是必要条件 / 底线，不是合格线。两条标准并存：相对线判定底线（不及格直接证伪），绝对线判定合格（做出对的味道才是合格）。

---

> 以下"修改总结"是给本 skill 设计合作者的迭代记录，**不属于产品方案主体**，最终交付的 idea.md 会删除此节。

## 修改总结（v3 → v4 · 混合处理 P2C 漂移诊断）

P2C 暴露了四处方向漂移，作者对每处给出了取舍判断，本版本是混合处理：

- **漂移 A · 从"专业纪律工具"滑向"大众友好工具"** —— **(c) 有意识方向调整**。
  - 已知方向：本 skill 的目标用户底色是 vibe / web coder（不是专业设计师），低门槛是有意为之。但同一份产物对专业设计师可作快速草稿用——**双轨服务**而非二元对立。
  - 改动：第一节增补"双轨服务对象"段；2.1 末尾"设计原则"那句被替换为更中性的"门槛设计原则：以推荐替代强制"，去掉过度软化的产品定调话语。

- **漂移 B · 产物从"资产"降级为"起点"** —— **(b) 部分回退到 v1 立场，并保留 v3 的双轨观察**。
  - 回退：第三节标题、定位段、第四节卖点段、第五节适用场景全部恢复"资产"主定位；删除"产物是用户后续手动精修的起点"等降级表述。
  - 保留 v3 的合理补充：承认在专精场景下的质量天花板、专业设计师可把产物当草稿——但这是"同一份资产在不同用户层级下的不同用法"，不是"产物本身就是半成品"。

- **漂移 C · 双层关系从"互相校准"变成"先后单向"** —— **(c) 双重保留**（作者无明确决定，指令"都保留"）。
  - 处理：2.2 把"互相校准"作为产品哲学层面的支点恢复（两层互为约束的语义），把 v3 的 4 步执行流程 + 单向标注作为该哲学的工程化落地。两者并置不矛盾——执行单向、语义双向耦合。
  - 2.3 末尾对引用规则的描述也同步改写：单向是为了不错乱，但语义上仍承载"每个 token 必须能溯源到体感法则"的双层耦合。

- **漂移 D · 成功标准从绝对降为相对** —— **(b) 部分回退到 v1 立场**。
  - 回退：第六节末尾恢复 v1 的绝对成功线"做出对的味道，做不到 skill 就没价值"作为合格标准。
  - 保留 v3 的"不输于直接喂图"——但**降级为底线 / 必要条件**，写明"仅作为下限基线，不是合格线"。两条并存：相对线判定底线，绝对线判定合格。

**v1 / v2 / v3 / v4 映射**：

| 章节 | v4 立场来源 |
|------|-------------|
| 第一节 产品定位 | v3 + 新增双轨说明 |
| 2.1 输入侧门槛 | v3（保持软门槛，删除过度软化的设计原则话语） |
| 2.2 处理侧 | v3 4 步流程 + v1 互相校准的产品哲学 并置 |
| 2.3 输出格式 | v3 |
| 第三节 形态选择 | v3 框架 + 回退资产主定位 |
| 第四节 vs 直接喂图 | v1 资产主张 + v3 卖点 #1 双层场景区分 |
| 第五节 适用场景 | v3 框架 + 删除"初稿"主场景、并置普通/专业用户用法 |
| 第六节 验证回路 | v3 + v1 绝对成功线（"对的味道"作为合格标准） |
