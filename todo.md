# TODO · 项目重构计划

> 2026-08-30 重立。项目形态从"方法论文档"升级为**思想层 + 工程层 + 评测层**三层作品。
>
> 目标不变：秋招面试作品，评价标准是"面试官 5 分钟内能看懂价值 + 能答住追问"。
>
> 本文件取代 2026-08-26 立的旧 Phase 1/2/3 结构。旧结构里已完成的部分归入"历史完成记录"，未完成的部分并入新 Phase。

---

## 当前状态（2026-09-13）

**收束阶段。** 评测层到此为止（12 条真实链 + 网页端到端 1 条、5 个解析 bug 修完、76 条结构测试、一次 VoyageGuard 回溯对照）。剩下的全是「摆成别人能看懂、能上手的样子」。

- [x] `README.md`（中）/ `README.en.md`（英）/ `LICENSE` / `requirements.txt`
- [x] `docs/case-voyageguard.md`（数据生成）/ `docs/evaluation.md` / `VALIDATION.md` 十七至二十
- [x] `web/index.html`（anthropic.com 设计系统）+ `web/chain_api.py` 无状态分步 + `web/server.py`；本地端到端 7 分 59 秒 / $0.072
- [x] `demo` 档（luna + deepseek-flash + glm-4-flash，三坐标各异，$0.08/链）
- [x] Vercel 包装层：`api/plan.py` `api/step.py` `api/_shared.py` `vercel.json` `.vercelignore`
- [x] 旧文件归档 `archive/retired-2026-09/`（jsx、旧 README、旧可视化页）
- [ ] 推 GitHub（保留旧历史）→ Vercel 建项目部署 → 填环境变量 → 线上跑一条完整链 → 线上链接进 README

**验证程度的实话**：auto 档完整验证；循环三出口只离线验了装置，真实运行没走完过第二轮；hitl 档按 09-08 转向不验（人工节点的价值不可证明）；跨厂商复判没做。

## Phase A · 地基重写

目标：任何 AI 冷启动进来，看到的是新定位而不是旧的文档项目。

- [x] **`.gitignore`** —— 加 `.env` / `runs/` / `__pycache__/` / `node_modules/` 等。**这是安全前置，必须最先做**
- [x] **`.env.example`** —— 五个 provider 的 key 占位，附选型依据
- [x] **`start_prompt.md`** —— 三层定位、模型配置与实测依据、界面设计语言、必读顺序更新
- [x] **`todo.md`** —— 本文件
- [x] **`CLAUDE.md`** —— 增补：模型配置纪律、预算纪律、API key 安全纪律。原有沟通规则和项目纪律不动
- [x] **`Liangyi.md` / `Liangyi.zh-CN.md`** —— 三处修正：
  - Quick Start 不再把过期的 `Liangyi.jsx` 当唯一入口（当前写着"读完这一个文件就能跑第一个项目"，而 jsx 里有 32 处 Qwen、35 处旧判据术语）
  - "演进路径"段的"对齐路径 × 语料文化"改为"冲突偏向 × 语料文化"
  - 开头"RLHF 训练出的保守性"改为不依赖算法披露的说法（landscape 的结论是头部厂商已不公布 RL 算法）
- [x] **`docs/liangyi-auto-variant.md`** —— 三处修正：
  - 5.2 节末尾"实证状态：方案 A 设计完成、尚未实战验证"已被 ExamSniper 的 6 次判定推翻，与第八节自相矛盾
  - 文件头"基于三次实战归纳"、第一节"只跑过两个场景三次实战" → 实际是 4 场景 6 次
  - 文末"最近更新：2026-04-26" → 文件内有多处 8-27 修订

---

## Phase B · 工作流骨架

目标：一条完整的 P0→P2 链能从头跑到尾，产出结构化执行记录。先做全自动模式，HITL 留到 Phase C。

**目录结构**

```
engine/
├── config.py          窗口定义、模型映射、provider 配置
├── providers.py       四家 API 适配（OpenAI 兼容格式，差异在 base_url/key/推理字段名）
├── window.py          窗口抽象 = 独立 messages 数组 + 模型绑定
├── steps.py           各步骤定义（输入、prompt、输出）
├── orchestrator.py    确定性编排器
├── trace.py           执行记录 + 推理过程捕获
└── prompts/           各步骤 prompt 模板（从 session-hygiene.md 的样板迁移）
```

**关键设计**

- [x] **窗口 = 独立 messages 数组**。session hygiene 从人肉纪律变成代码结构保证——单盲窗口的 messages 从空开始、只塞 idea.md，物理上不可能泄漏。可审计，面试时能指着代码说
- [x] **Provider 适配**。四家都是 OpenAI 兼容格式，差异只在 base_url、key、推理字段名（OpenRouter 用 `reasoning`，DeepSeek/Kimi/GLM 用 `reasoning_content`）
- [x] **推理过程捕获**。不只是展示——它验证一件此前无法验证的事：批判 agent 是真在批判还是在敷衍。特别是拆台那步，可对比 A1 与 A3 的思考差异，把"A1 的规范层会在关键处软化攻击"从理论推导变成实证
- [x] **空输出检测**。三家旗舰全是推理模型，`max_tokens` 给小了返回空 content（GLM-5.3 实测思考 2980 字符还没开始答）。必须给足预算并检测
- [x] **成本追踪**。每步记 token 与花费，累计到运行级别

**验收**：用一个 seed 跑通 P0→v5 全链，产出与实验归档同构的文件（P0-refined / P1A / P1B / idea-v1~v5 / 各步批判稿）+ 完整 trace.jsonl。调试期用 flash 档，成本控制在 $2 内。

---

## Phase C · 条件触发式 HITL

目标：把"人在哪里不可替代"做成产品机制，而不只是文档主张。

| 位置 | 类型 | 依据 |
|---|---|---|
| 2C-rollback | 必停 | 投资 skill 那次证明，不停就交付错东西 |
| 2D-fix | 必停 | 变种四场景证明，不停就卡死 |
| P0 精炼审 | 条件触发 | 精炼版与原话语义偏离超阈值才停 |
| 2A/2B-fix 范围扩大化检测 | 条件触发 | critic 攻击 X 但 AI 砍掉 X+Y+Z 时才停 |

- [x] 决策点全部可配置开关（0 / 2 / 4 三档预设，见 `gate.PRESETS`）
- [x] "范围扩大化检测"——把决策模拟器那次的教训做成机制，并用**那次的真实记录**
      验证：AI 原始反应（砍掉英文版）触发、人修正后的版本不触发
- [x] 触发时命令行展示分歧点，等人输入决定，写进 decision-log
- [x] 全自动模式下自动通过，但把"这里本来会问你"标记进 trace
- [x] **P1.0 专家角色生成 + 假对立检测**（计划外补的，见当前状态节）
- [x] **决策点摘要**（`digest.py`）——把判断收敛到"当初要 X、现在是 Y、认不认"

**验收**：同一 seed 用三档配置跑出可对比的结果。**剩这一项没做**。

#### 界面设计的一条教训（2026-08-31）

第一版界面把整篇 P2C 诊断摊开，Wenbo 的反馈是「我没有这个能力和资格去判断……
这几个实验里包含的领域都不是我擅长的领域」。

诊断：**判断层次错了**。界面在要求领域专家做设计评审，而方法论要的是意图所有者
做守护判断——docs 说的是「回退的标准是**这偏离了产品最初要解决的问题**」。

改成"你当初要 X、现在变成 Y、认不认"之后，同一个人同一个场景，反馈变成「我其实
就可以去判断……不算是那种特别技术性或者具有太高判断要求的问题」。

**含义**：决策点的判断层次错了，会把**有意图的人也变成橡皮图章**——他因为"我不懂"
而只能点接受，这看起来和"认真判断后同意"一模一样。这是「反滑坡规则」防的滑坡的
一种新形态，此前只在"人累了"这个维度被讨论过。

---

## Phase D · Trace 可视化

目标：面试官 5 分钟看懂的那个东西。

- [ ] 链条时间线：每步是谁、什么角色、花了多少钱多少时间
- [ ] 节点详情：点开看输入、输出、**推理过程**
- [ ] 决策点高亮：人在哪里介入、AI 想干什么被拦住
- [ ] idea 版本对比：v1→v5 逐版 diff，看见每条批判具体改动了哪句话

**技术**：静态 HTML + 内联 JS，不引入前端框架。**视觉改照 anthropic.com 设计系统**（2026-09-13 改定，见 `start_prompt.md` 第七节）；已由 `web/index.html` 实现。

**验收**：把"AI 想砍掉整个英文版被拦下"这类场景跑出来，图上一眼能看到那个节点。

---

## Phase E · 消融评测实验

**5 个场景**（按天然压力类型划分，seed 压到 2-4 句话）

| # | 类型 | 预期压力点 |
|---|---|---|
| 1 | 付费意愿存疑的 C 端工具 | 商业逻辑 |
| 2 | 技术用户的开发者工具 | 表达精确性 |
| 3 | 多方利益冲突的平台功能 | 方向漂移 |
| 4 | 前提可能就错的新概念 | 前提假设 |
| 5 | 成熟赛道的差异化产品 | 护城河 |

**注意**：右列是预期不是设计目标。seed 要写得自然、不刻意埋雷，否则是循环论证。跑完若某步没抓到预期问题，那是真实的负面结果，**如实展示**。

#### 场景方向（2026-08-31 定，待写 seed）

| # | 类型 | 方向 | 状态 |
|---|---|---|---|
| 1 | 付费意愿存疑的 C 端工具 | 帮人管理各种订阅、提醒该退订哪些 | 待写 |
| 2 | 技术用户的开发者工具 | 线上报错诊断助手 | **已有** `scenarios/dev-diagnose.yaml` |
| 3 | 多方利益冲突的平台功能 | 电商平台的商家评分申诉机制 | 待写 |
| 4 | 前提可能就错的新概念 | 每天早上告诉你今天该穿什么 | 待写 |
| 5 | 成熟赛道的差异化产品 | 又一个会议纪要工具 | 待写 |

**写 seed 的硬约束**：2-4 句话，只说清楚**用户是谁、痛点是什么、产品大概什么
形态**。不写商业模式（让链条去挖）、不写技术方案（超出方法论作用域）、不列功能
清单（让链条去展开）。**每个 seed 都要像一个真实产品经理会随口提的想法**——
为了让某一步"抓到东西"而刻意埋雷，就是循环论证。

**角色怎么来**：先跑 P1.0 自动生成 + 假对立检测，我审质量，不合格的手写替换。
生成结果写进场景文件，所以实验可复现性不受影响。这一步顺带**用真实场景验证
P1.0**（此前只在一个测试 seed 上验过）。

**执行顺序**：写 4 个 seed（不花钱）→ Wenbo 审「这像不像真人会提的想法」→
跑 P1.0 生成角色（约 $0.1）→ 审角色 → 定稿。

**实验设计**

- [ ] 消融结构：v0（单 AI 一次性出方案，基线）→ v1（P1 融合）→ v2→v3→v4→v5

#### 评分设计 · 已改为成对比较（2026-08-30，实测推翻原设计）

**原设计（绝对打分）实测不可用**：5 个维度各 1-10 分，五个裁判对同一组文档给出
从 −4 到 +4 的相反结论。自一致性测试查出根因——**裁判自身噪声 4-6 分，远大于
要测的信号 1 分**。换裁判、换更贵的模型都救不了，病在方法不在模型。

**现设计（成对比较）**：

- 不问"这份值几分"，问"**这两份哪份更好**"——不需要评分锚点，一致性显著更高
- 每对做**正反两次**（X 在 A 位一次、B 位一次）抵消位置偏差
- 逐维度判胜负 + 一个总体判断，胜率 ≥70% 才算"更好"，40-60% 记为"区分不出"
- 实测效果：同样文档同样裁判，12 次比较**零分歧**，两裁判均 3/6 选 A 位（无位置偏好）

**规模与成本**：5 场景 × 5 对相邻版本比较 × 2 裁判 × 3 轮 × 正反 2 次 ≈ 300 次，
10 路并发约 15 分钟、约 $1。比原计划的 180 次绝对打分**更快也更便宜**。

**并发是关键**：评分任务互相独立，串行跑几小时、并发跑几分钟。这个差别决定了
"跑完看结果再调实验设计"可不可行。已在 `engine/judge.py` 实现。

**实现**：`engine/judge.py` 的 `run_pairwise()` / `win_rate()`，
prompt 在 `engine/prompts/judge_pairwise.md`。绝对打分的实现保留
（`score_document`），作为"方法对照"的证据留档。
- [x] **裁判选型已解决**（2026-08-30）——anchor 从 gpt-5.6-luna 换成
      claude-sonnet-5 之后，GPT 和 GLM 都空出来了：两者坐标不同（A2·B1 / A3·B2）、
      都没参与生产。原先那个"裁判不能给自己打分"的设计错误自动消失
- [x] **primary 配置换成 claude-sonnet-5**（A1·B1）——与 session-hygiene 的默认
      推荐组合（Claude×4 + DeepSeek×2 + Kimi×1）完全一致。理由不只是能力：
      docs 里"为什么 P0 与执笔要用 A1"的全部论证都基于 A1 写的，且六次实验全是
      Claude 执笔，用 A2 会在"实验证据"与"产品实现"之间留断层。实测成本 5 场景
      $5.17，绝对值在预算内。对照组翻转为 contrast_a2（测换掉冲突偏向档位后结论稳不稳）

<details><summary>裁判选型的原始问题（已解决，留档）</summary>

- ~~**裁判选型待重定**（2026-08-30 发现的设计错误）~~：原计划 gpt-5.6-luna + glm-5.3，
      但 luna 是 anchor 位、**参与了生产**——裁判不能给自己打分。而且实测 GLM-5.3
      思考量极大（P0 一步就 3259 tokens），成本是预估的数倍。
      重定原则：裁判必须来自**没参与生产的厂商**（不是没参与的模型——同厂商共享
      规范层与语料先验，会带相同偏好）。生产用了 OpenAI + DeepSeek + Moonshot，
      所以裁判候选只剩 Anthropic（A1·B1）与智谱（A3·B2）。
      成本要重算：按 5 场景 × 6 版本 × 2 裁判 × 3 次 = 180 次评分，
      claude-haiku + glm-5.3 组合约 $13，占总预算近一半。
      可选优化：重复次数 3 降到 2、或换更便宜的档位。Phase E 启动前定

</details>

#### 实测单步成本（2026-08-30 Phase B 跑批实测，取代此前的估算）

| 模型 | 角色 | 单步均价 | 单步耗时 | 备注 |
|---|---|---|---|---|
| gpt-5.6-luna | anchor（执笔+批判） | $0.006 | 15-60s | 思考少、产出长，最适合工作流 |
| deepseek-v4-pro | 专家B / 拆台 | $0.009 | 150s | 推理模型，思考 5000+ |
| kimi-k3 / k2.5 | 单盲 | $0.011 | 28s | 单次调用，可接受 |
| glm-5.3 | 应急 anchor | $0.038 | 250-560s | **思考 12000+，慢 11 倍贵 8 倍产出还少一半；执笔步骤直接超时（300s 重试三次全挂）** |

**关键结论**：国产三家旗舰全是重推理模型，做需要高频调用的执笔位非常吃亏。
这反过来印证 luna 的选型——OpenAI 官方定位它为 "fast, cost-efficient，适合
high-volume、latency-sensitive 的 agentic workflow"，实测确实如此。
**选模型不是选最强的，是选和任务形态匹配的**：批判任务要深度思考，执笔任务
要快速产出长文本，这是两种不同需求。

据此重算单链成本：luna anchor 9 步 + DeepSeek 2 步 + Kimi 1 步 ≈ **$0.08/链**
（远低于此前 $0.41 的估算）。链条部分完全不是成本瓶颈，**评分环节才是**。
- [ ] 乱序编号、每对打分跑 3 次看稳定性
- [ ] 评分维度：完整性 / 可行性 / 风险覆盖 / 目标用户清晰度 / 自洽性
- [ ] **附加对照组一**：2 场景 × HITL 三档（0/2/4 个决策点）——直接回答"人到底有没有用"
- [ ] **附加对照组二**：2 场景 × Claude Haiku（A1）vs luna（A2）——测换坐标后结论稳不稳

**节奏**：第一轮只跑 3 个场景验证流程、暴露设计问题；稳了再补到 5 个跑正式数据。

**面试话术**：
- "评测自己的方法论时，用方法论自己的 divergence 原则防止自评偏差"
- 关于自造场景：这个领域**没有现成 benchmark**——查到的 IdeaBench / LigBench / LiveIdeaBench / AI Idea Bench 全是科研 idea 方向，产品 idea 没有权威基准。另有数据可引：一项对 61 个 LLM-ideation 研究的综述显示 75% 有评估环节、其中 46% 用 LLM 评估，**单一 LLM 裁判是主流做法且有已知偏差**——双裁判不同坐标正是对这个缺陷的回应

---

## Phase F · 面试叙事层

- [ ] **统一 Web 界面**：顶部体验入口（面试官输入任意想法，试跑模式全自动快速出结果，在该停的地方标"这里本来会问你"）+ 下方消融实验完整回放
- [ ] **GitHub 重构上传**（删旧 v1.1.0 内容，保留 repo 和 16 star）
- [ ] **面试 Q&A 预演**：预判追问（"比直接问 AI 好在哪""怎么验证的""成本多少""模型换代了怎么办""你凭什么说需要这些人工决策点"）
- [x] **`Liangyi.jsx` 去留裁决**：已退役归档（2026-09-13），`web/index.html` 是主入口
- [ ] **对外 README 的文档索引表**：要不要让变种和实验露面（旧 todo 遗留的待裁项）
- [ ] **API key 轮换**：讨论期暴露过的 5 个 key 全部 revoke 重发

---

## 决策记录

### 2026-08-30（本轮重定位）

- **项目升级为三层作品**（思想层 + 工程层 + 评测层）。理由：目标岗位要"能自己把东西做出来的人"，Moonshot JD 原话"一次性的 demo 人人能做出来"、DeepSeek 写"能用 vibe coding 写代码，不一定需要技术背景"——门槛不是科班是产出物
- **agent 化就是那次 P4**。旧 todo 记着"六次实验没一次跑到 P4，面试官问'产品做出来了吗'答不上"。把方法论自己实现出来，这个缺口就补上了，而且产品就是方法论本身
- **主力模型 gpt-5.6-luna**（A2·B1）。依据：维度 A 是厂商级属性（Model Spec 全线适用），luna 与 GPT 旗舰坐标相同，选档位是能力选择不是坐标选择。能力已用 P0 忠实精炼 / P2A 批判 / P2C 漂移诊断三项实测，全过，且 P2C 那项在候选里质量最好成本最低
- **DeepSeek / Kimi / GLM 用各自官方 API 的旗舰**（deepseek-v4-pro / kimi-k3 / glm-5.3），OpenRouter 只用于 Claude 和 GPT
- **千问排除**——项目手上有可用的 key，但按"维度 A 未公开不进入任何窗口"的硬规则不用。**规则要能约束自己才算数**，这本身是面试素材
- **不用总调度 agent**，用确定性编排器。理由：流程步骤固定（Anthropic 的 Building Effective Agents 原则）、可复现是消融实验的合法性基础、且撞方法论红线三"不做多 Agent 自动协商"
- **不用 batch 模式**——链条严格串行（v2 依赖 P2A 输出），无法提前打包。靠选便宜模型控成本
- **HITL 不能只留 P2D 一个**。关键区分：四次变种实验测的是"流程会不会跑不下去"（只有 P2D 会卡死），两次本体跑测的是"跑完的东西对不对"（2C 和 2A 位置人的介入改变了产品）。后者才是方法论核心命题——**只留 P2D 会让产品精确地犯自己方法论警告的错误**
- **决策点数量由实验测出**，不拍脑袋。做成可配置开关，跑 0/2/4 三档对照
- **消融实验主线全关 HITL**——保可复现，剔除"是方法论好还是夏文博厉害"的混淆变量
- **场景从 6 个减到 5 个**，按天然压力类型划分而非业务形态（原 6 个里"外卖差评回复"与"餐厅促销"重复、"会议纪要"与"活动页审批"重复）
- **Loop 只加四个**：P2→P1（已有）、批判 agent 内部自检（1-2 轮上限）、P3→P2 回流（显式声明）、二次 P2 轮（可选深度模式）。不加 P1→P0 / P2→P0 / P3→P0
- **Web 界面视觉照 anthropic.com 设计系统**（2026-09-13 改定，jsx 已退役）
- **总预算 $15，上限 $30**

### 2026-08-27 及更早（历史）

- 2026-08-26：目标重定位为面试作品；优先级地基先行；评测场景不带公司名只带业务形态；GitHub 旧版本直接删旧传新；实验原始文件夹保留不动
- 2026-08-27：**维度 A 判据整体更换**（对齐算法路径 → 冲突偏向）。起因：九家复核发现头部厂商已不公布 RL 算法、可验证奖励与生成式奖励模型按任务分流而非选路线、GRPO 成为公共基础设施。**换判据不换机制**
- 2026-08-27：判据同时看训练方法与出厂后叠加的规范层（system prompt、规范文档、运行时分类器都算，因为使用者绕不过这一层且它比训练算法更可观测）
- 2026-08-27：**维度 A 未公开的模型完全不进入方法论**。对外话术"我的方法论只用能追到一手训练材料的模型"
- 2026-08-27：维度 B 不变（语料文化由预训练决定，不随对齐配置调整）
- 2026-08-27：参考组合不写具体版本号，只写厂商 + "当前旗舰"
- 2026-08-27：实验收获一律不删、不融进本体、只贴面试价值标签
- 2026-08-27：Kill shot 分级 / 终止条件 / 议题敏感度撤出面试主线（变种自造问题的解）
- 2026-08-27：风格提炼 skill 实验 freeze，不跑 P3.1.1
- 2026-08-27：三条本体修改落地——P2D 触发条件改为"必触发 + 可选触发"、P3 隐含前提显性化（deliverable 是文档时退化为转译+校验）、立场三精确化为"做出系统内部无法自判的决策"

---

## 面试主证据清单

Phase F 写叙事时直接取用。

**1. 反证故事**（变种四次实验唯一的高价值产出）
跑了四次实验试图把人从方法论里拿掉，撞到的所有墙都指向同一件事：**能委托的是系统内部的局部判断（批判取舍、是否回退），不能委托的是站在系统外的边界判断（要不要终止、前提是不是错了、这轮该不该退出）**。为绕过这个结论打了两轮补丁（终止条件、Kill shot 分级），绕不过去。价值在于这是**证伪尝试**——不是找支持核心主张的证据，而是试着推翻它，没推翻掉。

**2. 四步批判不冗余**（Agent 评测 Round 2 + 两次本体跑）
P2A（工程 Lead）全在问"能不能验证"（统计效度、κ 撑不撑得住 go/no-go），P2B（零上下文工程师）全在问"能不能执行"（JSONPath 指哪个字段、工单对接 Jira 还是自研）。没有 P2A 得到有细节没护栏的原型，没有 P2B 得到有护栏没细节的 PPT。回答"你搞四步是不是过度设计"。

**3. P2C 是整条链最值钱的一步**（投资 skill 本体跑）
抓到 v1→v3 累积漂移：产品身份从"投资助手"漂成"防呆引擎"、推荐功能从核心降为"非核心但保留"、卫星仓 25%→10%。每步单看都在响应真实批判，加起来变成另一个产品。原话："如果没有那次回退，最后交付的会是一份通过审查的合规文档，不是 Wenbo 想要的投资助手。"

**4. 两个旗舰模型对同一事实给出相反答案**（投资 skill P3.1.1 校验）
DeepSeek 与 Claude Opus 4.8 校验同一件事（SpaceX 6/12 是否上市），都联网检索、都自信、都给细节、结论完全相反。回答"为什么要两个 AI，一个不够吗"——不需要任何理论。

**5. AI 面对批判会把攻击范围扩大化**（决策模拟器 round-2）
critic 只说"双语同口径在心理类无成功先例"，Claude-2 直接把整个英文版砍了；critic 只说"海外华语是 7 个碎片市场"，它直接收窄到北美华人。两次都是人拦下来的。

**6. 规则能约束自己才算数**（2026-08-30 新增）
手上有可用的千问 API key，但方法论的硬规则说"维度 A 未公开的模型不进入任何窗口"，所以没用。这证明选型规则不是写着好看的。

---

## 未决问题

- 5 个场景的具体 seed 表述（Phase E 前打磨）
- ~~`Liangyi.jsx` 最终去留~~ → 已归档（2026-09-13）
- 对外 README 的文档索引表要不要让变种和实验露面（Phase F 定）
- **OpenRouter 403 封锁**——充值后立即出现，账户级（最简单的请求也拒、Claude 和
  GPT 都拒、`provider_name` 为 null 说明是网关层挡的）。正式实验必须等它恢复

### 待裁定 · Phase B 跑批撞出的两条方法论级观察

按第十二章的操作判据，这两条是「流程自己撞出来的现象」而非「我主动加进去的
设计」，属于可以上升的那一类。但要不要上升、怎么改，由 Wenbo 定。两条都是
单场景证据，远不到本体修订门槛。

**一 · 「A3 不适合做 P0 精炼」的机制和 docs 写的不一样**

`liangyi-workflow-refined.md` 第六章说 A3「会主动推理和扩展」。实测三个模型跑
同一个 P0（原 seed 约 100 字）：

| 模型 | 坐标 | 产出 | 实际行为 |
|---|---|---|---|
| gpt-5.6-luna | A2·B1 | 124 字 | 干净，保持原文两段结构 |
| glm-5.3 | A3·B2 | 305 字 | **内容完全忠实**，但自己加了一节「改动说明」 |
| glm-4-flash | A3·B2 | 128 字 | 加标题、两段并一段、引入原文没有的「我」 |

关键在 glm-5.3：推理过程里它逐条核对了硬性要求、甚至讨论了「忠实度信号」，
**内容一个字没扩展**。它扩展的是**任务边界**——思考的最后阶段自己决定
"Format: refined text + short note on changes"。

更准确的表述可能是：不是「A3 会扩展内容」，是「**重推理模型倾向于自己决定输出
格式**」。而 A2 权限优先的特点恰恰是**没授权的事不做**。结论没错，机制说错了。

这条是靠推理过程抓到的——只看最终输出只会记录「GLM 多写了东西」，看不到它是在
哪一步、基于什么理由做的决定。

**二 · 选型要看「任务形态」，不只看坐标和能力**

第三章的乘法模型里 `match(A,B)` 现在定义为「AI 和角色是否匹配」。实测提示它可能
还要包含「模型响应特性和这一步的任务形态是否匹配」：

| | P1.4 综合这一步 | | |
|---|---|---|---|
| | 耗时 | 思考 tokens | 产出 |
| gpt-5.6-luna | 49 秒 | 170 | 8,275 字 |
| glm-5.3 | **564 秒** | **12,220** | 3,924 字 |

glm-5.3 更在 2A-fix（长文档修订）直接超时。国产三家旗舰全是重推理模型，思考量大
在**批判类**任务上是优点，在**执笔类**任务上是灾难。这不是能力不够——它的批判
质量并不差，是任务形态不匹配。

**三 · 一条对 Phase E 有用的附带观察**

冒烟测试用最弱的 flash 档跑 P2C，产出**完全符合 docs 定的产出规范**（是漂移暴露
不是错漏列表、结尾向人提问不替人决定，甚至自发用了正例里那个句式）。分析深度确实
不如旗舰，但「该产出什么形状的东西」弱模型也照做了。

含义：结构合规和分析深度是两件可分开的事。评分裁判的任务形态更接近前者，所以
不一定要用最贵的模型——可能让那笔 $13 的评分预算降下来。Phase E 启动前用真实
评分任务验证一次。顺带印证了 MAD 论文那条：judge 不必比 debater 强。
