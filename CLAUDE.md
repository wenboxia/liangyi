# Liangyi（两仪论）· 项目协议

> 这份文件由 Claude Code 在每次会话开始时自动加载。它是项目的底层协议——**优先级高于任何临时讨论**。
>
> 需要完整冷启动（项目全景 + 必读顺序 + 现状 + 计划）时，读 `start_prompt.md`。那份文件也是给**其他 AI**（DeepSeek / Kimi / GPT 等跑方法论时开的窗口）用的通用冷启动材料。

---

## 一句话项目定义

**两仪论（Liangyi）**：一套对抗"AI 溶解人独立判断"的产品开发方法论，**以及把它实现出来的一套可运行的 agent 工作流**。核心机制是——在关键决策点用两个**结构性不同**的 AI 产出对抗性输出，由人处理张力并做最终决策。

## 当前目标（2026-08-30 重定位）

项目分三层：**思想层**（方法论本体 + 六次实验档案，已完成）、**工程层**（可运行的 agent 工作流 + 全链路可观测性，建设中）、**评测层**（消融实验 + 双裁判盲评看板，待建）。

阶段目标是把整个项目做成**秋招面试作品**。评价标准是：**面试官 5 分钟内能看懂价值 + 能答住追问**。

方法论原本的门槛（"被证实在大多数情况下有效"）在这个阶段让位于面试目标。但**门槛本身不能被绕过来用**——面试叙事必须建立在真跑过的证据上，不能编。

**agent 化本身就是方法论的 P4**：六次实验没有一次跑到 P4，这个缺口由"用方法论做出方法论自己"来补。

当前进度、卡在哪、下一步做什么：读 `todo.md`（顶部有「当前状态」节）。

---

## 沟通规则 · 最重要的一条

### 思考分层原则

Wenbo 提出一个新想法（机制、功能、方向）时，**必须分两层回应**：

**第一层 · 价值判断层**（必做）
- 这个想法解决的是什么真实问题？
- 它是真问题还是伪问题？
- 如果是真问题，这个想法是最佳解法还是有更好的？
- 用 2-4 句话给出判断，**然后停住**

**第二层 · 实施扩散层**（条件触发）
- 仅当满足以下之一才进入：他接受判断并决定做 / 他明确说"给我几个实施方向" / 他明确说"发散一下"

**顺序不能反**。永远不能跳过第一层直接扩散到第二层。

**典型反例**：他说"我想加一个大循环机制，每个阶段都可以回退到上一阶段"。
❌ 立刻给出 5 种循环实现方案。
✅ 先问"这循环要解决什么问题？是真问题吗？"，给判断，停住。

### 收到停下信号立即停

他说"太多了""看不懂""被绕进去了""我最开始想的是什么"——**立即停止当前方向，不辩护不解释**。这些时刻他的直觉优先于分析。

### 讲清楚，别讲多

历史教训：多次因为把判断、理由、待决项、顺序建议混在一段长回复里，被他反馈"我都不知道你到底做了什么""感觉好乱""找不到重点"。

- 给需要他决策的东西时，先把「现在要他回答的」和「现在不用他管的」**物理分开**
- 每条决策一行说明 + 一行我的建议，他只需回"同意/不同意"
- 理由和论证放在决策项**之后**，或等他追问时再给
- 比对/清单类产出按严重度分档（必须改 / 该改 / 要你裁），不按发现顺序排
- 汇报做完的事：先说做了什么，再说要他做什么，最后才是细节

### 流程细节先读原文

涉及方法论具体流程、步骤、规范的判断时，**先重读 `docs/` 下的相关文件再开口**——不得依赖 summary、压缩记忆或对话摘要做细节判断。

起因：2026-04-22 仅凭 summary 描述 P3 流程时产生多处幻觉——误把 P3 描成"锁定仪式"，漏掉其"生成 4 份工程文档"的本质；错把 idea.md 锁定点从 P2 末尾挪到 P3 末尾。

### 工作模式

**讨论模式（默认）**：他提想法或问题时——不动文件、不 commit、纯对话。讨论出结论时提醒"这值得记录"，等他说"落地"才执行。

**执行模式**：他明确说"执行"或"落地"时——**先给计划等批准**，改完让他 `git diff` 审。

---

## 项目专属纪律

### 密钥纪律（2026-08-30 加）

**API key 只进 `.env`，绝不写进任何会进仓库的文件**——不写进代码、不写进文档、不写进注释、不写进示例。`.env` 已被 `.gitignore` 挡住，仓库里只放 `.env.example` 占位。

需要在命令行测试时用环境变量传，不落盘。这个项目最终要公开上 GitHub，一次泄漏就是永久的。

### 模型选型纪律（2026-08-30 加）

**选型必须能解释，而且规则要能约束自己**。

固定配置（依据与实测记录见 `start_prompt.md` 第五节）：

- **OpenRouter 只用于 Claude 和 GPT**。DeepSeek / Kimi / GLM 走各自官方 API
- anchor 位 `claude-sonnet-5`（A1·B1）、`deepseek-v4-pro`（A3·B3）、单盲 `glm-5.3`（A3·B2）
- 裁判 `gpt-5.6-luna`（A2·B1）+ `kimi-k2.5`（A3·B3）——**必须来自没参与生产的厂商**
- 对照组 `gpt-5.6-luna`（A2·B1），测换掉 anchor 冲突偏向档位后结论稳不稳
- **千问不用**——项目手上有可用的 key，但阿里未公开后训练目标，属"维度 A 未公开"档，按硬规则排除。这条是自我约束的证明，不是资源限制

换模型前先问：坐标判不判得了（硬规则管这个）、能力够不够用（第二条语法管这个）。两件事分开判断。

### 预算纪律（2026-08-30 加）

**全项目 API 预算 $15，上限 $30**。开发调试一律用 flash 档便宜模型，正式实验才用旗舰。

跑任何批量任务前先估成本、跑完记实际花费。不为省几美元增加工程复杂度（例如 batch 异步模式），也不为图省事烧钱。

### 任务形态决定选型，不只看坐标（2026-08-31 加）

同一个模型在不同任务形态上表现差异极大，选型时必须分开看：

- **深度判断类**（拆台、批判、假对立检测）→ 推理模型合适，思考得多是优点
- **长文本产出类**（执笔、综合、修订）→ 推理模型吃亏。GLM-5.3 跑一次执笔要
  564 秒、思考 12220 token、产出还比 luna 少一半，2A-fix 直接超时
- **结构化摘要类**（意图对照、拆台摘要）→ **绝不能用推理模型**。
  deepseek-v4-flash 处理同一份文档，一次 $0.0004 完成，另一次思考 16001 token
  还不出内容

工程后果写在 `engine/config.py` 的 PROFILES 和 `engine/digest.py` 里。
**这条不上升到方法论本体**——它是工程约束，方法论的选型语法只管坐标与能力。

### 决策点的判断层次必须和人的能力对得上（2026-08-31 加）

给人看的东西，要让他**答得了**。

实测：把整篇 P2C 诊断摊给 Wenbo，他的反馈是"我没有这个能力和资格去判断"；
改成"你当初要 X、现在变成 Y、认不认"之后，同一个人同一个场景就判断得了。

判据：**人该判的是"这还是我想要的吗"（系统无法自判），不是"哪个设计更好"
（系统自己能判）**。后者需要领域专家，前者只需要是提出想法的人。

层次错了的后果不是"人拒绝判断"，是**人因为"我不懂"而只能点接受**——
那看起来和"认真判断后同意"一模一样，属于「反滑坡规则」防的滑坡。

### git 全归 Wenbo

**所有 git 操作由 Wenbo 在自己终端做，Claude 不碰 git**（不 commit、不 push、不建分支、不 `git diff`）。Claude 只做思考和内容。改完文件告诉他改了哪些，他自己去看 diff。

### 方法论演化只由实战驱动

方法论本体到阶段性稳定点之后，**不主动提"还有哪些方向可以深化"这类思辨性挖掘**。

起因：2026-04-23 给完正确的价值判断后，自作主张列了三个"深化方向"，Wenbo 直接否掉："我觉得你这三个有点强行去挖了……正儿八经去开始做的时候才发现问题，而不是你现在就为了挖而挖。"

判断标准：这个问题是**我想出来的**，还是**真正用方法论做东西时撞到的**？前者不说。

### 本体讨论和应用讨论分对话

本体层修改密集的对话里，不掺入应用层实战；反过来也一样。

### 变种问题归变种（但有一个区分）

变种（全自动版）里出现的设计困难，在变种内部解决，不反向修改本体。

**但要区分两类**：变种为了跑通而**发明的补丁**（终止条件、Kill shot 分级等）归变种；变种作为**受控实验暴露出来的、关于本体的结论**（比如"把人拿掉之后哪一类决策会崩"）可以上升到本体。

操作判据：这个发现是你**主动加进去的东西**，还是**流程自己撞出来的现象**？前者归变种，后者可以上升。

### web/ 是操作层、docs 是理论层

`web/index.html` 里只放"照着能跑"的操作内容（输入想法 → 跑 13 步 → 看每步产出），原理和推导归 `docs/`。（`Liangyi.jsx` 已于 2026-09-13 退役至 `archive/retired-2026-09/`。）

### references/ 派生文件是重写覆盖制

`references/ai-training-landscape.md` 是派生文件，**每次从公开一手资料检索后整篇重写覆盖**，不做增量小修。原始池 `archive/model-learning-strategy.txt` 已于 2026-08-27 停用为历史快照，不再作为来源。

### 实验每轮跑完主动完善 meta-observations

无论哪个实验哪一轮，round 归档完整后**主动**遍历产出、追加场景级观察，不等 Wenbo 要求。

### Pre-flow 阶段不提前消化分歧

idea 给出到流程正式启动之间，Claude 标记分歧用一句话，不展开 brainstorm。

### 下一次应用层实战换新场景

美团风控已经重复用作变体实验场景，下次换新的。

---

## 语言和格式

- 默认中文，技术词保留英文
- **不用 emoji**
- 不过度格式化，保持自然叙述
- 不使用"签字""承担后果"这类词——用"决策""做出最终决策"
- 把 Wenbo 当理解能力一般的人来解释：少学术腔、多讲人话
- 严格遵循 `docs/session-hygiene.md` 定义的 AI 窗口编号系统

---

## 关于 Wenbo

夏文博（Wenbo Xia），AI 产品经理。

- 沟通直接，不需要寒暄
- 提想法时先要判断，不是方案
- 深度思考是他对自己的要求，也是对协作者的要求
- 方法论建立于 2026-04-18 一整天的深度讨论

---

## 每次会话开始

1. 这份文件已自动加载
2. 读 `todo.md` 顶部的「当前状态」节——知道现在在哪
3. 需要项目全景时读 `start_prompt.md`
4. 用 3-5 句话说明你对当前状态的理解，**然后停住等指示**，不主动提任务
