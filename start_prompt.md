# Start Prompt · 冷启动引导

> **用途**：任何 AI 进入这个项目时读的第一份文件。读完 + 按指引读完必读文件后，应该能说清项目是什么、现在在哪、下一步做什么。
>
> **两个使用场景**：
> - **Claude Code**：根目录的 `CLAUDE.md` 会自动加载（协议 + 纪律），本文件提供完整项目全景。让它读一遍即可。
> - **其他 AI**（跑方法论时开的 DeepSeek / Kimi / GPT 窗口）：那些地方没有自动加载机制，把本文件全文粘过去。

---

## 一、这个项目是什么

**两仪论（Liangyi）**：一套对抗"AI 溶解人独立判断"的产品开发方法论，以及**把它实现出来的一套可运行的 agent 工作流**。

工作流本身：一个想法进去，一份经过四轮对抗审查的方案出来。对抗式方案生成 → 投资人批判 → 零上下文单盲复审 → 方向漂移检测与回退 → 前提拆台分级；写与审来自不同厂商、不同对齐路线，具备多出口回退循环，关键节点提供 HITL 人工判断。

人工决策步骤不是方法论的成本，是方法论的价值所在——这一条是本体立场，网页与 CLI 的 `hitl` 档就是它的落地。**但项目不再试图用实验证明这一条**：2026-09-08 评测层转向（`docs/evaluation-layer-pivot.md`）之后，「人工节点有没有价值」被判定为不可证明的问题，只验 `auto` 档跑得通、跑得对。

### 项目分三层（2026-08-30 定位，2026-09-15 状态）

| 层 | 内容 | 状态 |
|---|---|---|
| **思想层** | 方法论本体（`docs/`）+ 六次实验档案（`experiments/`） | 已完成，阶段性稳定 |
| **工程层** | 可运行的 13 步链条 + auto / hitl 两档 + 全链路 trace（`engine/`）+ 在线入口（`web/` + `api/`） | **已完成并上线**：https://liangyi-five.vercel.app |
| **评测层** | 一次 VoyageGuard 回溯对照 + 20 条方法论合规检验 + 83 条结构保证测试 | **2026-09-08 转向后到此为止**。消融实验、双裁判盲评、HITL 对照组全部取消 |

**为什么要有工程层**：方法论如果只是文档，它就只是一篇长文章。做成能跑的东西之后，session hygiene 从人肉纪律变成代码保证（单盲窗口的上下文物理上不可能泄漏），而且方法论第一次真正跑到了 P4（做出产品），**这个产品就是方法论本身**。

**当前目标**：整个项目是秋招面试作品，评价标准是"面试官 5 分钟内能看懂价值 + 能答住追问"。交付形态已经定稿（仓库 + 线上入口）。方法论原本的门槛（"被证实在大多数情况下有效"）让位于这个目标——但**门槛本身不能被绕过来用**，面试叙事必须建立在真跑过的证据上，不能编。

**项目所有者**：夏文博（Wenbo Xia），AI 产品经理。他是唯一决策者，你是协作者。

---

## 二、必读文件（按此顺序）

1. `./CLAUDE.md` —— 项目底层协议。**沟通规则最重要**：思考分层原则（他提想法时先做价值判断、停住、等回应，绝不直接扩散实施方案）、停下信号立即停、讲清楚别讲多、流程细节先读原文、执行前先给计划、git 与部署的授权边界
2. `./todo.md` —— 当前工作计划 + **顶部的「当前状态」节**。先读这份的顶部，才知道现在在哪
3. `./README.md` —— 对外入口（中文；`README.en.md` 英文）。访客看到的第一份东西，话术以它为准
4. `./engine/PIPELINE.md` —— 13 步各是谁在跑、两个档位差在哪、全自动时谁顶替了人。**由配置导出**，比任何手写描述准
5. `./docs/README.md` —— 文档导览
6. `./docs/session-hygiene.md` —— 人肉跑方法论时的操作规范（AI 窗口编号、每步用哪个 AI、硬规则、参考组合）
7. `./docs/liangyi-workflow-refined.md` —— 方法论本体（三条立场 + 认知场论 + 为什么是 2 个 AI + divergence 优先 + 四轴框架 + P0-P4 完整工作流 + 核心纪律）
8. `./references/ai-training-landscape.md` —— **底模坐标 landscape**。维度 A 的判据和各厂商现状，2026-08 快照
9. `./engine/README.md` —— 工程层说明（链条结构、断点续跑、P1.0、实测踩过的坑）
   与 `./engine/VALIDATION.md`（**用方法论判据检验实现**，20 条检验，失败的照原样留着）
10. `./docs/case-voyageguard.md` + `./docs/evaluation.md` + `./docs/evaluation-layer-pivot.md` —— 评测层的全部：一个回溯案例、凭什么信它、为什么不再往下做
11. `./web/DEPLOY.md` —— 在线入口怎么跑、怎么部署、限次怎么做的
12. `./docs/longterm-and-reference.md` —— 长期观察 + 理论借鉴。按需读
13. `./docs/liangyi-auto-variant.md` —— 全自动变种。**涉及变种时读**

---

## 三、过去 · 方法论怎么演化到今天

这一节存在的理由：不知道这些，新窗口会以为现在的样子是天生如此，从而在讨论里犯已经犯过的错。

**2026-04-18** · 方法论诞生日。一整天深度讨论，推导出认知场论类比、"为什么是 2 个 AI"的三重证明（信息论 + 认知科学 + SVD）、divergence 优先于能力、session hygiene。

**2026-04 ~ 05** · 六次实验。四次全自动变种（把人工决策点全拿掉，看哪里崩）+ 两次本体完整跑。

**2026-08-26** · 目标重定位为面试作品。

**2026-08-27** · **维度 A 的判据整体更换**——方法论至今最大的一次修订：

- 原判据是"对齐路径"四档（A1 Constitutional+RLAIF / A2 传统 RLHF / A3 GRPO 家族 / A4 RLVR）
- 复核九家厂商一手材料后发现该分法失效：头部厂商不再公布 RL 算法（A1/A2 无法观测）；可验证奖励与生成式奖励模型变成按任务类型分流而非选路线（A3/A4 不互斥）；GRPO 成为公共基础设施（A3 失去区分度）
- 新判据是"**冲突偏向**"——任务要求和其他约束冲突时模型默认倒向哪边。三档 + 一个未公开档：**A1 规范优先**（Claude / Gemini）、**A2 权限优先**（GPT）、**A3 任务优先**（DeepSeek / Kimi / GLM）、**未公开**（Qwen / 豆包 / MiniMax）
- 新增硬规则：**维度 A 未公开的模型不进入方法论的任何窗口**。理由不是模型不好，是方法论要求选型依据可解释
- **换的是判据，不是机制**

**2026-08-30** · **项目形态重定位**——从"方法论文档"升级为"思想层 + 工程层 + 评测层"三层作品。同日完成 API 选型验证（见第五节）。

**2026-08-30 ~ 09-06** · 工程层建起来：13 步链条、窗口隔离、trace、断点续跑、P1.0 自动生成角色 + 假对立检测、条件触发检测器、决策点摘要。五个场景各跑一条正式档全自动链。

**2026-09-08** · **评测层转向**（`docs/evaluation-layer-pivot.md`）。放弃「用胜率证明 HITL 有价值」——裁判噪声大于信号、人的判断价值不可证明。评测改成一次回溯对照：拿一个已经做完的项目（VoyageGuard）倒回原始想法重跑，和当年真实返工清单对照。四档 HITL 合并为 `auto` / `hitl` 两档，条件触发检测器改为所有档位后台跑、只记录不打断。

**2026-09-10** · 回溯跑完；自查抓到五个同类解析 bug（拿模型措辞当契约）并修掉；循环出口 A、B 用真实判定书离线驱动验证（出口 C 结构性死锁真实运行不可达）；拆台判定不稳定这条发现记入 VALIDATION 检验十八。评测层到此为止。

**2026-09-13** · **收束为可交付形态**：README 中英、案例与评测文档、`web/index.html` 改照 anthropic.com 设计系统、无状态分步 API、`demo` 档、旧文件归档；推 GitHub（旧 v1.1.0 历史保留为祖先）、Vercel 部署、Upstash KV 限次；线上跑通一条完整 13 步。

**2026-09-14** · 产品面话术按机制重写（不为易读抹平）；网页支持 auto / hitl 两档切换，hitl 在两个必停点出决定卡；运行控制补齐停止 / 继续 / 重试这一步 / 下载运行记录。

**2026-09-15** · 状态机图定稿：auto / hitl 各一张纵向 mermaid 图，单脚本生成，README 上下叠、网页左右并排。**交付形态定稿。**

---

## 四、现在 · 当前状态（2026-09-15）

**交付完成。** 没有排期中的待办；后续改动只由实际使用撞出的问题驱动。

**链条 13 步**：P0 → **P1.0（生成专家角色 + 假对立检测）** → P1A ∥ P1B → P1.4（v1）→ P2A → 2A-fix（v2）→ P2B（零上下文单盲）→ 2B-fix（v3）→ P2C（方向漂移诊断）→ **2C-rollback（v4）** → P2D（前提拆台 + 致命论据分级）→ **2D-fix（v5 或判回 P1）**。循环出口三条：回 P1 整链重跑 / 回 P2 重压批判链 / 结构性死锁退出，两轮封顶。

**两档**：`--mode auto`（一次不停，可复现）/ `--mode hitl`（2C-rollback、2D-fix 两处必停，三选一 + 可附指令）。早期 `off / minimal / advised / full` 四档只剩 `LEGACY_MODES` 映射供 `--resume`。条件触发检测器（P0 忠实性、范围扩大化）在所有档位后台跑、只记录不打断。

**线上入口** https://liangyi-five.vercel.app ：
- 跑 `demo` 档完整 13 步（约 8–10 分钟、$0.08），步骤列表显示每步耗时 / 花费 / 输入输出，点开看详情
- 网页也支持 auto / hitl 切换；hitl 到停点出决定卡（上下文 + 三选一 + 可附指令 + 材料包下载），选「接受」不多花一次模型调用
- 运行控制：停止 / 继续 / 重试这一步 / 下载运行记录（全在浏览器端，服务端无状态，运行目录在请求体里来回传）
- 限次：每 IP 每天 3 条链，Upstash KV 计数；`?owner=<LIANGYI_OWNER_TOKEN>` 不受限
- 部署：Vercel 单函数 `api/index.py`（`pyproject.toml` 声明入口），GitHub push 自动部署
- 状态机图：`docs/flow-auto.mmd` / `docs/flow-hitl.mmd`，由 `docs/visualizations/build_flow_mmd.py` 一次生成两张并写进 README 与网页

**验证程度的实话**：auto 档完整验证；循环出口 A、B 只离线验了装置，真实运行没走完过第二轮；出口 C（结构性死锁）编排器不传 `overlap`，真实运行不可达、无测试用例；hitl 档按 09-08 转向不验（人工节点的价值不可证明）；跨厂商复判没做；拆台判定同输入多次跑会给出不同分级（检验十八，未解决）。

**已退役（2026-09-13 起）**：`Liangyi.jsx`、旧 README（`Liangyi.md` / `Liangyi.zh-CN.md`）、`docs/visualizations/` 下的旧页面、`build_flow_svg.py`，全部在 `archive/retired-2026-09/`，**只作历史快照，不要采信其中的模型选型和坐标标注**。

---

## 五、模型配置与选型依据

全部经过 2026-08-30 的实测验证（API 可用性 + 能力测试），不是纸面推演。配置在 `engine/config.py` 的 `PROFILES`。

**正式档 `primary`**（CLI 跑批用；单链约 $1.2、26–44 分钟）：

| 窗口 | 模型 | 走哪个 API | 坐标 |
|---|---|---|---|
| 执笔者 / P0 精炼 / P1.0 / P1 专家A / P2A / P2C | **claude-sonnet-5** | OpenRouter | A1·B1 |
| P1 专家B / P2D 拆台 | **deepseek-v4-pro** | DeepSeek 官方 | A3·B3 |
| P2B 单盲复审 | **glm-5.3** | 智谱官方 | A3·B2 |
| 条件触发检测器（影子模式） | gpt-5.6-luna，每检测点 3 票取多数 | OpenRouter | 读长文档出 JSON，不能用推理模型（09-05 从 deepseek-v4-flash 换来） |
| 决策点摘要 | gpt-5.6-luna | OpenRouter | 读长文档 + 结构化输出，**不能用推理模型** |

**在线档 `demo`**（网页访客跑的；单链约 $0.08、8–10 分钟）：

| 槽位 | 模型 | 坐标 |
|---|---|---|
| anchor | gpt-5.6-luna | A2·B1 |
| divergent_a | deepseek-v4-flash | A3·B3 |
| divergent_b | glm-4-flash | A3·B2 |

选它的理由：三个坐标各不相同（`smoke` / `debug` 档两个批判位同坐标，拿去演示「跨维度对抗」说不过去），而且快、便宜。正式档不适合在线——单盲一步最长跑过 1199 秒。

**跨维度硬规则检查**（正式档）：Claude（A1·B1）× 4 + DeepSeek（A3·B3）× 2 + GLM（A3·B2）× 1，三个不同坐标，严格版满足。`session-hygiene.md` 的默认推荐组合 P2B 用 Kimi，工程实现取的是它列出的等价替代 GLM（单盲主要激活 context 轴，不依赖坐标 divergence；GLM 便宜、中文先验更贴）。

**为什么 anchor 位用 Claude Sonnet 5**（2026-08-30 从 gpt-5.6-luna 换过来）：docs 里「为什么 P0 与执笔要用 A1」的全部论证都基于 A1 规范优先写的，且六次实验全是 Claude 执笔——用 A2 会在"实验证据"与"产品实现"之间留一道断层。实测成本 5 场景 $5.17。

**评测层已停**：`config.py` 里的 `JUDGES`、`contrast_a2` 档只留档，没有任何运行用到它们，也不会再用。

**为什么没有千问**：阿里未公开后训练目标、也无公开规范文本，属"维度 A 未公开"档。项目手上**有可用的千问 key**，但按硬规则不用——规则要能约束自己才算数。

**为什么不用 batch 模式**：链条严格串行（v2 依赖 P2A 的输出），无法提前打包提交。靠选便宜模型控成本，不为省几美元增加异步复杂度。

**预算**：原则「全项目 $15，上限 $30」。实际：正式档 10 条链 $12.05 + 回溯与循环验证若干，已接近上限，**正式档不再跑批**；线上 demo 链每条 $0.08，由限次控制。

**工程注意**：DeepSeek-V4-Pro / Kimi-K3 / GLM-5.3 全是推理模型，`max_tokens` 给小了会返回**空 content**（GLM-5.3 实测思考 2980 字符还没开始回答）。必须给足预算并检测空输出。

---

## 六、六次实验 · 它们各自证明了什么

**四次全自动变种**（把人工决策点全拿掉，看哪里崩）：

| 实验 | 场景类型 | 撞到了什么 |
|---|---|---|
| 2026-04-23 美团风控 | B端/专业用户 | P2D-fix 死循环，人工退出。**首次暴露"边界决策不可委托"** |
| 2026-04-24 Agent 评测平台 | B端/技术用户 | 加终止条件后第一次仍触发回 P1；Round 2 首次跑通完整 v1→v5 |
| 2026-04-26 ExamSniper | C端/学生 | Kill shot 分级新旧规则对照。**6 次判定 6 次判回 P1，零分歧** |
| 2026-04-26 人生决策模拟器 | C端/城市白领 | 变种首次跑到 P3 四份文档。实际是"变种骨架 + 人保留决策权"的混合形态 |

**两次本体完整跑**（每个决策点都由 Wenbo 做）：

| 实验 | 交付物 | 关键观察 |
|---|---|---|
| 2026-05-02 设计风格提取 skill | `.skill.md` | **P3 退化模式首现**；P2D 在前三步都有重大修改时仍有补抓价值。已 freeze |
| 2026-05-23 个人投资 skill | `SKILL-2.md`（40KB，在用） | **观察密度最高的一次**，`scenario-observations.md` 必看 |

**面试主证据**（完整原文在 `todo.md`）：

1. **反证故事**——跑了四次实验试图把人拿掉，四个场景全部卡在同一个位置（P2D-fix 的边界判断），而前面的批判取舍 AI 做得不差。为绕开这个结论打了两轮补丁，绕不过去。这是**证伪尝试**，不是找支持证据
2. **四步批判不冗余**——P2A 问"能不能验证"、P2B 问"能不能执行"，两条批判完全正交
3. **P2C 是整条链最值钱的一步**——投资 skill 那次抓到 v1→v3 累积漂移，如果没回退，交付的会是"通过审查的合规文档"而不是他要的投资助手
4. **两个旗舰模型对同一事实给出相反答案**——DeepSeek 与 Opus 4.8 校验 SpaceX 是否上市，都联网、都自信、结论相反
5. **AI 面对批判会把攻击范围扩大化**——critic 只说"双语同口径没先例"，Claude-2 直接把整个英文版砍了。人不在场，产品价值就这么一刀一刀丢掉
6. **回溯对照**（2026-09，工程层证据）——VoyageGuard 原始想法 514 字跑一遍，14 条原子点，复判守住 8 / 收窄 4 / 未守住 2（封存初判 7 / 5 / 2，差一条），7 条真实返工项命中 4。数字由脚本从运行记录生成，见 `docs/case-voyageguard.md`

**一个重要区分**（讨论 HITL 时必须知道）：四次变种实验测的是"流程会不会**跑不下去**"，结论是只有 P2D-fix 会卡死；两次本体跑测的是"跑完的东西**对不对**"，结论是 2C-rollback 和 2A-fix 位置人的介入直接改变了产品。**后者才是方法论的核心命题**——AI 溶解人的判断，不是让流程崩溃，而是让流程顺利跑完、只是跑出来的不是你要的。所以人工决策点不能只留 P2D 一个。这是立场，不是待证明的假设——项目不再为它设计实验。

---

## 七、界面设计语言

**2026-09-13 改定**：不再沿用 jsx 的水墨风，改照 **anthropic.com 的设计系统**（Wenbo 指定，取值是打开量的，不是凭感觉）：

- **配色**：页面底 `#FAF9F5` / 正文与深色面 `#141413` / 次级面 `#F0EEE6` / 边框 `1px rgba(20,20,19,.1)` / 次级文字 `#87867F`、`#B0AEA5` / **强调色** `#D97757`（按下 `#C6613F`）
- **字体**：**正文衬线、标题无衬线**（和常见做法相反，这是它最好认的特征）。正文 20px / 行高 28px，标题 700。字体栈用回退：serif → Georgia / Songti SC / Noto Serif SC；sans → -apple-system / PingFang SC / Arial
- **形状**：8px 圆角为主，大块 16 / 24px；无阴影、无滤镜
- **元素**：只保留一个简洁的单色太极图标。毛笔滤镜、朱砂印章、罗马数字标号全部去掉
- **文案**：机制点名，不为易读抹平（「四轮批判链」「多出口循环」「HITL 节点」要写出来）；不用 emoji

实现在 `web/index.html`。状态机图用 mermaid `neutral` 主题，两张纵向图左右并排。

---

## 八、目录结构

```
liangyi/
├── CLAUDE.md                  · 项目协议（Claude Code 自动加载）
├── start_prompt.md            · 本文件
├── todo.md                    · 工作计划 + 当前状态 + 决策记录（必读）
├── README.md / README.en.md   · 对外 README（中/英），含两张状态机图
├── LICENSE / requirements.txt / pyproject.toml（Vercel 入口与依赖声明）
├── .env.example               · API key 模板（.env 已被 gitignore 挡住）
├── vercel.json / .vercelignore · Vercel 函数时长、路由；挡住 .env 与档案
├── api/
│   ├── index.py               · Vercel 单入口（/、/api/plan、/api/step）
│   └── _shared.py             · 限次（KV / 内存）、owner 令牌
├── web/                       · 在线入口：静态页 + 无状态分步 API（部署说明见 web/DEPLOY.md）
│   ├── index.html             · 页面（anthropic.com 设计系统；两档切换、决定卡、运行控制）
│   ├── chain_api.py           · 一步一个请求，运行目录在请求体里来回传
│   └── server.py              · 本地开发服务器
├── engine/                    · 工程层（13 步链条 + auto/hitl 两档，83 条结构保证）
│   ├── PIPELINE.md            · 哪一步谁跑、两档差在哪（配置导出）
│   ├── README.md              · 工程层说明
│   └── VALIDATION.md          · 用方法论判据检验实现（20 条）
├── scenarios/                 · 场景 seed（角色可手写，不写则 P1.0 自动生成）
├── runs/                      · 运行产出（gitignore）
├── retrospective/             · VoyageGuard 回溯对照的封存判据与结论
├── references/
│   └── ai-training-landscape.md · 底模坐标 landscape（重写覆盖制、2026-08 快照）
├── docs/                      · 方法论权威文档 + 评测层文档
│   ├── README.md / session-hygiene.md / liangyi-workflow-refined.md
│   ├── liangyi-auto-variant.md / longterm-and-reference.md
│   ├── case-voyageguard.md / evaluation.md / evaluation-layer-pivot.md
│   ├── flow-auto.mmd / flow-hitl.mmd · 两张状态机图源
│   └── visualizations/        · 生成脚本：build_case_doc.py（案例文档）、build_flow_mmd.py（状态机图）
├── experiments/               · 六次实验完整归档（原始文件保留、不压缩不删除）
└── archive/                   · 归档区——retired-2026-09/ 里是退役的 jsx、旧 README、旧可视化页、SVG 图脚本
```

---

## 九、读完之后你该做什么

1. 用 3-5 句话向 Wenbo 报告你对当前状态的理解（**用自己的话，不复述文件**）
2. 报告里应体现：知道三层定位、知道项目已交付且评测层已停、知道思考分层原则、知道停下信号
3. **停住等指示**——不主动提议任务，不主动开始执行 todo 里的任何一项，尤其不要提「补个 HITL 对照组」这类已经取消的方向

---

## 十、高频踩坑提醒

- 他提新想法 → 先做价值判断（真问题还是伪问题）→ **停住等回应** → 他确认后才谈实施
- 他说"太多了 / 看不懂 / 被绕进去了" → **立即停止当前方向，不辩护**
- 给他决策项 → 先把"现在要他回答的"和"现在不用他管的"分开，每条一行 + 一行建议，理由放后面
- 涉及方法论流程细节 → **先读 `docs/` 原文**，不靠对话记忆
- 执行类动作（改文件 / 建文件）→ **先给计划等批准**，改完告诉他改了哪些
- **API key / token 只进 `.env`，绝不写进任何会进仓库的文件；也绝不由 AI 代填进任何服务**——环境变量由他自己推
- 讲话方式：默认中文、不用 emoji、不过度格式化、少学术腔多讲人话
- **git 与部署**：本项目已授权 commit / push / Vercel 部署，push 前一句话说明；删分支、force push、改历史仍归他
