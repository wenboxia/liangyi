# Start Prompt · 冷启动引导

> **用途**：任何 AI 进入这个项目时读的第一份文件。读完 + 按指引读完必读文件后，应该能说清项目是什么、现在在哪、下一步做什么。
>
> **两个使用场景**：
> - **Claude Code**：根目录的 `CLAUDE.md` 会自动加载（协议 + 纪律），本文件提供完整项目全景。让它读一遍即可。
> - **其他 AI**（跑方法论时开的 DeepSeek / Kimi / GPT 窗口）：那些地方没有自动加载机制，把本文件全文粘过去。

---

## 一、这个项目是什么

**两仪论（Liangyi）**：一套对抗"AI 溶解人独立判断"的产品开发方法论，以及**把它实现出来的一套可运行的 agent 工作流**。

核心机制：在关键决策点用两个**结构性不同**的 AI（跨底模坐标）产出对抗性输出，由**人**处理张力并做最终决策。人工决策步骤不是方法论的成本，是方法论的价值所在。

### 项目分三层（2026-08-30 重新定位）

| 层 | 内容 | 状态 |
|---|---|---|
| **思想层** | 方法论本体（`docs/`）+ 六次实验档案（`experiments/`） | 已完成，阶段性稳定 |
| **工程层** | 可运行的 agent 工作流 + 条件触发式 HITL + 全链路可观测性（`engine/`） | 13 步链条跑通，21 项结构保证通过 |
| **评测层** | 消融实验 + 双裁判盲评看板 | 待建 |

**为什么要有工程层**：方法论如果只是文档，它就只是一篇长文章。做成能跑的东西之后，三件事同时成立——session hygiene 从人肉纪律变成代码保证（单盲窗口的上下文物理上不可能泄漏）、消融实验从"人肉开七个窗口跑六遍"变成"跑一个脚本"、以及方法论第一次真正跑到了 P4（做出产品），而**这个产品就是方法论本身**。

**当前目标**：把整个项目做成秋招面试作品。评价标准是"面试官 5 分钟内能看懂价值 + 能答住追问"。方法论原本的门槛（"被证实在大多数情况下有效"）在这个阶段让位于面试目标——但**门槛本身不能被绕过来用**，面试叙事必须建立在真跑过的证据上，不能编。

**项目所有者**：夏文博（Wenbo Xia），AI 产品经理。他是唯一决策者，你是协作者。

---

## 二、必读文件（按此顺序）

1. `./CLAUDE.md` —— 项目底层协议。**沟通规则最重要**：思考分层原则（他提想法时先做价值判断、停住、等回应，绝不直接扩散实施方案）、停下信号立即停、讲清楚别讲多、流程细节先读原文、执行前先给计划、git 全归他
2. `./todo.md` —— 当前工作计划 + **顶部的「当前状态」节**。先读这份的顶部，才知道现在在哪
3. `./docs/README.md` —— 文档导览
4. `./docs/session-hygiene.md` —— 操作规范（AI 窗口编号、每步用哪个 AI、硬规则、参考组合）
5. `./docs/liangyi-workflow-refined.md` —— 方法论本体（三条立场 + 认知场论 + 为什么是 2 个 AI + divergence 优先 + 四轴框架 + P0-P4 完整工作流 + 核心纪律）
6. `./references/ai-training-landscape.md` —— **底模坐标 landscape**。维度 A 的判据和各厂商现状，2026-08 快照
7. `./engine/README.md` —— 工程层说明（链条结构、HITL 三档、实测踩过的坑）
   与 `./engine/VALIDATION.md`（**用方法论判据检验实现**，14 条检验，
   其中三条是「实现违反了方法论，后果与预言一致」的实证）
8. `./docs/longterm-and-reference.md` —— 长期观察（14 条，带面试价值标签）+ 理论借鉴。按需读
9. `./docs/liangyi-auto-variant.md` —— 全自动变种。**涉及变种或消融实验时读**

---

## 三、过去 · 方法论怎么演化到今天

这一节存在的理由：不知道这些，新窗口会以为现在的样子是天生如此，从而在讨论里犯已经犯过的错。

**2026-04-18** · 方法论诞生日。一整天深度讨论，推导出认知场论类比、"为什么是 2 个 AI"的三重证明（信息论 + 认知科学 + SVD）、divergence 优先于能力、session hygiene。

**2026-04 ~ 05** · 六次实验。四次全自动变种（把人工决策点全拿掉，看哪里崩）+ 两次本体完整跑。

**2026-08-26** · 目标重定位为面试作品。

**2026-08-27** · **维度 A 的判据整体更换**——方法论至今最大的一次修订：

- 原判据是"对齐路径"四档（A1 Constitutional+RLAIF / A2 传统 RLHF / A3 GRPO 家族 / A4 RLVR）
- 复核九家厂商一手材料后发现该分法失效：头部厂商不再公布 RL 算法（A1/A2 无法观测）；可验证奖励与生成式奖励模型变成按任务类型分流而非选路线（A3/A4 不互斥）；GRPO 成为公共基础设施（A3 失去区分度）
- 新判据是"**冲突偏向**"——任务要求和其他约束冲突时模型默认倒向哪边。三档 + 一个未公开档：**A1 规范优先**（Claude / Gemini）、**A2 权限优先**（GPT）、**A3 任务优先**（DeepSeek / Kimi / GLM）、**未公开**（Qwen / 豆包 / MiniMax）
- 新增硬规则：**维度 A 未公开的模型不进入方法论的任何窗口**。理由不是模型不好，是方法论要求选型依据可解释
- **换的是判据，不是机制**

**2026-08-30** · **项目形态重定位**——从"方法论文档"升级为"思想层 + 工程层 + 评测层"三层作品。同日完成 API 选型验证（见第五节），确定全部模型配置与预算。

---

## 四、现在 · 当前状态

**Phase C（条件触发式 HITL）基本完成**。完整进度见 `todo.md` 顶部。

链条 13 步：P0 → **P1.0（生成专家角色 + 假对立检测）** → P1A/P1B → P1.4 →
P2A → 2A-fix → P2B → 2B-fix → P2C → **2C-rollback（必停）** → P2D →
**2D-fix（必停）**。

`--hitl` 三档：`off` 全自动（消融实验主线用）/ `minimal` 两个必停点 /
`full` 再加两个条件触发点（P0 忠实性、范围扩大化）。

**已知仍过期、待后续处理**：`Liangyi.jsx`（2835 行双语，维度 A / 窗口分配 / 版本号全是 2026-04 快照）和 `docs/visualizations/liangyi_field_alive_v5.html`。两个文件顶部都有显著过期声明，**读到它们时不要采信其中的模型选型和坐标标注**。注意：jsx 有可能在 Phase F 直接退役（被新的 Web 界面取代），但**它的视觉设计语言要被继承**（水墨风格，见第七节）。

---

## 五、模型配置与选型依据

全部经过 2026-08-30 的实测验证（API 可用性 + 能力测试），不是纸面推演。

| 窗口 | 模型 | 走哪个 API | 坐标 |
|---|---|---|---|
| 执笔者 / P0 精炼 / P1 专家A / P2A / P2C | **claude-sonnet-5** | OpenRouter | A1·B1 |
| P1 专家B / P2D 拆台 | **deepseek-v4-pro** | DeepSeek 官方 | A3·B3 |
| P2B 单盲复审 | **glm-5.3** | 智谱官方 | A3·B2 |
| 评分裁判一 | gpt-5.6-luna | OpenRouter | A2·B1 |
| 评分裁判二 | kimi-k2.5 | Kimi 官方 | A3·B3 |
| A2 对照组（2 场景） | gpt-5.6-luna | OpenRouter | A2·B1 |
| 条件触发检测器 | deepseek-v4-flash | DeepSeek 官方 | 判断题，输出短 |
| 决策点摘要 | gpt-5.6-luna | OpenRouter | 读长文档 + 结构化输出，**不能用推理模型** |
| 开发调试 | deepseek-v4-flash / glm-4-flash | 官方 | 便宜跑通流程 |

**跨维度硬规则检查**：Claude（A1·B1）× 4 + DeepSeek（A3·B3）× 2 + GLM（A3·B2）× 1，
**三个不同坐标**，严格版满足。这与 `session-hygiene.md` 的默认推荐组合一致。

**为什么 anchor 位用 Claude Sonnet 5**（2026-08-31 从 gpt-5.6-luna 换过来）：
docs 里「为什么 P0 与执笔要用 A1」的全部论证都基于 A1 规范优先写的，且六次实验
全是 Claude 执笔——用 A2 会在"实验证据"与"产品实现"之间留一道断层。实测成本
5 场景 $5.17，绝对值在预算内。换过来还顺带解决了一个设计错误：原先 luna 既做
anchor 又当裁判，等于自己给自己打分；现在 GPT 和 Kimi 都空出来做裁判了。

**裁判必须来自没参与生产的厂商**（不是"没参与的模型"）——同厂商共享规范层与
语料先验，会带相同偏好。

**为什么没有千问**：阿里未公开后训练目标、也无公开规范文本，属"维度 A 未公开"档。项目手上**有可用的千问 key**，但按硬规则不用——规则要能约束自己才算数。

**为什么不用 batch 模式**：链条严格串行（v2 依赖 P2A 的输出），无法提前打包提交。靠选便宜模型控成本，不为省几美元增加异步复杂度。

**预算**：全项目约 $26，上限 $30（含一轮重跑预留）。单链约 $1.04。

**工程注意**：DeepSeek-V4-Pro / Kimi-K3 / GLM-5.3 全是推理模型，`max_tokens` 给小了会返回**空 content**（GLM-5.3 实测思考 2980 字符还没开始回答）。必须给足预算并检测空输出。

---

## 六、六次实验 · 它们各自证明了什么

**四次全自动变种**（把人工决策点全拿掉，看哪里崩）：

| 实验 | 场景类型 | 撞到了什么 |
|---|---|---|
| 2026-04-23 美团风控 | B端/专业用户 | P2D-fix 死循环，人工退出。**首次暴露"边界决策不可委托"** |
| 2026-04-24 Agent 评测平台 | B端/技术用户 | 加终止条件后第一次仍触发回 P1；Round 2 首次跑通完整 v1→v5 |
| 2026-04-26 ExamSniper | C端/学生 | Kill shot 分级新旧规则对照。**6 次判定 6 次判回 P1，零分歧** |
| 2026-04-26 人生决策模拟器 | C端/城市白领 | 变种首次跑到 P3 四份文档。实际是"变种骨架 + 人保留决策权"的混合形态 |

**两次本体完整跑**（每个决策点都由 Wenbo 做）：

| 实验 | 交付物 | 关键观察 |
|---|---|---|
| 2026-05-02 设计风格提取 skill | `.skill.md` | **P3 退化模式首现**；P2D 在前三步都有重大修改时仍有补抓价值。已 freeze |
| 2026-05-23 个人投资 skill | `SKILL-2.md`（40KB，在用） | **观察密度最高的一次**，`scenario-observations.md` 必看 |

**面试主证据**（完整原文在 `todo.md`）：

1. **反证故事**——跑了四次实验试图把人拿掉，四个场景全部卡在同一个位置（P2D-fix 的边界判断），而前面的批判取舍 AI 做得不差。为绕开这个结论打了两轮补丁，绕不过去。这是**证伪尝试**，不是找支持证据
2. **四步批判不冗余**——P2A 问"能不能验证"、P2B 问"能不能执行"，两条批判完全正交
3. **P2C 是整条链最值钱的一步**——投资 skill 那次抓到 v1→v3 累积漂移，如果没回退，交付的会是"通过审查的合规文档"而不是他要的投资助手
4. **两个旗舰模型对同一事实给出相反答案**——DeepSeek 与 Opus 4.8 校验 SpaceX 是否上市，都联网、都自信、结论相反
5. **AI 面对批判会把攻击范围扩大化**——critic 只说"双语同口径没先例"，Claude-2 直接把整个英文版砍了。人不在场，产品价值就这么一刀一刀丢掉

**一个重要区分**（讨论 HITL 时必须知道）：四次变种实验测的是"流程会不会**跑不下去**"，结论是只有 P2D-fix 会卡死；两次本体跑测的是"跑完的东西**对不对**"，结论是 2C-rollback 和 2A-fix 位置人的介入直接改变了产品。**后者才是方法论的核心命题**——AI 溶解人的判断，不是让流程崩溃，而是让流程顺利跑完、只是跑出来的不是你要的。所以人工决策点不能只留 P2D 一个。

---

## 七、界面设计语言（Phase D/F 要用）

新的 Web 界面**继承 `Liangyi.jsx` 的视觉语言**（Wenbo 2026-08-30 指定），即使 jsx 本身退役：

- **配色**：宣纸 `#F5F1E8` / 暖宣纸 `#FBF7ED` / 墨 `#1A1614` / 中墨 `#3D3530` / 淡墨 `#6B5D4F` / 极淡 `#C8BFB0` / 朱砂 `#A02C2C` / 青黛 `#3E5D6C`
- **字体**：`Cormorant Garamond` + `Noto Serif SC`（衬线正文）、`JetBrains Mono`（代码）
- **质感**：SVG 滤镜做毛笔效果（`feTurbulence` + `feDisplacementMap`），墨迹晕染用 `feGaussianBlur`
- **元素**：太极图 hero、墨点（激活/未激活两态）、波浪下划线、罗马数字标阶段（O / I / II / III / IV）

---

## 八、目录结构

```
liangyi/
├── CLAUDE.md                  · 项目协议（Claude Code 自动加载）
├── start_prompt.md            · 本文件
├── todo.md                    · 工作计划 + 当前状态 + 决策记录（必读）
├── .env.example               · API key 模板（.env 已被 gitignore 挡住）
├── Liangyi.md / .zh-CN.md     · 对外 README（英/中）
├── Liangyi.jsx                · 旧可视化入口（⚠️ 内容过期、Phase F 定去留）
├── engine/                    · 工程层（13 步链条 + HITL 三档，21 项结构保证）
│   ├── README.md              · 工程层说明
│   └── VALIDATION.md          · 用方法论判据检验实现（14 条）
├── scenarios/                 · 场景 seed（角色可手写，不写则 P1.0 自动生成）
├── runs/                      · 运行产出（gitignore）
├── references/
│   └── ai-training-landscape.md · 底模坐标 landscape（重写覆盖制、2026-08 快照）
├── docs/                      · 方法论权威文档
│   ├── README.md / session-hygiene.md / liangyi-workflow-refined.md
│   ├── liangyi-auto-variant.md / longterm-and-reference.md
│   └── visualizations/liangyi_field_alive_v5.html （⚠️ 内容过期）
├── experiments/               · 六次实验完整归档（原始文件保留、不压缩不删除）
└── archive/                   · 归档区——确定不会再用到的东西，想清理时可整个删掉
```

---

## 九、读完之后你该做什么

1. 用 3-5 句话向 Wenbo 报告你对当前状态的理解（**用自己的话，不复述文件**）
2. 报告里应体现：知道三层定位、知道当前 Phase、知道思考分层原则、知道停下信号
3. **停住等指示**——不主动提议任务，不主动开始执行 todo 里的任何一项

---

## 十、高频踩坑提醒

- 他提新想法 → 先做价值判断（真问题还是伪问题）→ **停住等回应** → 他确认后才谈实施
- 他说"太多了 / 看不懂 / 被绕进去了" → **立即停止当前方向，不辩护**
- 给他决策项 → 先把"现在要他回答的"和"现在不用他管的"分开，每条一行 + 一行建议，理由放后面
- 涉及方法论流程细节 → **先读 `docs/` 原文**，不靠对话记忆
- 执行类动作（改文件 / 建文件）→ **先给计划等批准**，改完告诉他改了哪些
- **API key 只进 `.env`，绝不写进任何会进仓库的文件**
- 讲话方式：默认中文、不用 emoji、不过度格式化、少学术腔多讲人话
- **git 全部由他在终端自己做，你不碰**
