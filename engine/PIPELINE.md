# 链条全景 · 哪一步谁在做

这份文件回答三个问题：**每一步是哪个模型在跑**、**三个 HITL 档位差在哪**、
**全自动时谁顶替了人的位置**。

表格内容由 `engine/config.py` 和 `engine/steps.py` 直接导出，不是手写的。
改了配置这份文件就过期了，重新导出即可。

---

## 主表 · 13 步

`跑` = AI 自动执行　`⏸` = 必定停下问人　`⚠` = 检测器报警才停

| # | 步骤 | 角色 | 模型 | 坐标 | off | minimal | full |
|---|---|---|---|---|---|---|---|
| 1 | P0 | 忠实精炼原始想法 | claude-sonnet-5 | A1·B1 | 跑 | 跑 | ⚠ |
| 2 | P1.0 | 生成两个对抗角色 | claude-sonnet-5 | A1·B1 | 跑 | 跑 | 跑 |
| 3 | P1A | 专家 A 出方案 | claude-sonnet-5 | A1·B1 | 跑 | 跑 | 跑 |
| 4 | P1B | 专家 B 出方案 | deepseek-v4-pro | A3·B3 | 跑 | 跑 | 跑 |
| 5 | P1.4 | 执笔 · 融合成 v1 | claude-sonnet-5 | A1·B1 | 跑 | 跑 | 跑 |
| 6 | P2A | 怀疑论投资人批判 | claude-sonnet-5 | A1·B1 | 跑 | 跑 | 跑 |
| 7 | 2A-fix | 执笔 · 改出 v2 | claude-sonnet-5 | A1·B1 | 跑 | 跑 | ⚠ |
| 8 | P2B | 零上下文单盲复审 | glm-5.3 | A3·B2 | 跑 | 跑 | 跑 |
| 9 | 2B-fix | 执笔 · 改出 v3 | claude-sonnet-5 | A1·B1 | 跑 | 跑 | ⚠ |
| 10 | P2C | 知情复审 · 查方向漂移 | claude-sonnet-5 | A1·B1 | 跑 | 跑 | 跑 |
| 11 | **2C-rollback** | 执笔 · 回退漂移，改出 v4 | claude-sonnet-5 | A1·B1 | 跑 | **⏸** | **⏸** |
| 12 | P2D | 拆台专家 · 质疑前提 | deepseek-v4-pro | A3·B3 | 跑 | 跑 | 跑 |
| 13 | **2D-fix** | 执笔 · 框架内改 or 回 P1 | claude-sonnet-5 | A1·B1 | 跑 | **⏸** | **⏸** |

P2B 那一行的「零上下文」是底线规则：那个窗口永远收不到任何历史，只拿到当前
方案正文。它模拟的是一个陌生读者，用来暴露知情者看不见的盲区。

---

## 三个档位分别对应什么情形

| 档位 | 停几次 | 用在哪 |
|---|---|---|
| `off` | 0 | 消融实验的对照组底座。全自动，可复现，跑批不用人在场 |
| `minimal` | 2 | 只开两个必停点。方法论说「边界决策不可委托」，指的就是这两个 |
| `full` | 2 + 最多 3 次条件触发 | 上面两个，加 P0 精炼审、两处范围扩大化检测。检测器不报警就不停 |

`full` 比 `minimal` 多出来的三次是**条件触发**，不是必停：
- **P0-review**（第 1 步）——精炼版偏离原意时才叫人
- **scope-creep**（第 7、9 步）——改着改着范围膨胀了才叫人

所以 `full` 实际停几次取决于检测器，可能就是 2 次，也可能 5 次。

---

## 全自动时，谁顶替了人的位置

**`claude-sonnet-5`（A1·B1），以 `scribe`（执笔者）窗口的身份。**

第 11 步和第 13 步在 `--hitl off` 下不会停，执笔窗口直接把决策**写进产出**：

- **2C-rollback**：执笔者自己判断诊断指出的哪些漂移要回退、哪些是合理演进
- **2D-fix**：执笔者按 kill shot 分级规则判定「框架内改出 v5」还是「回 P1 重做」

第二条有明确判定标记，结果记在 `run.json` 的 `verdict` 字段（`produced-v5` /
`back-to-p1`）。第一条没有独立判定，回退决策融在 v4 正文和 `decision-log.md` 里。

**这不是权宜之计**。`liangyi-workflow-refined.md` 第 28 行的修订写着：四次变种
实验里「是否回退，AI 自主判断的质量并不差」，`longterm-and-reference.md` 第 138
行记着 AI 自判 2C **4 次全对**。人不可委托的是**边界决策**，方法论把它精确到了
2D-fix 一个点。

---

## 模型槽位 · 为什么是这三个

配置在 `PROFILES["primary"]`，三个槽位对应三个底模坐标：

| 槽位 | 模型 | 坐标 | 承担 |
|---|---|---|---|
| `anchor` | claude-sonnet-5 | A1·B1 | 执笔、专家 A、投资人批判、知情复审 |
| `divergent_a` | deepseek-v4-pro | A3·B3 | 专家 B、拆台 |
| `divergent_b` | glm-5.3 | A3·B2 | 零上下文单盲 |

**拆台必须由 A3 做**。方法论的论证是：A1（规范优先）的底模里有一层独立于任务的
价值排序，让它扮演恶意拆台，它会在最关键那一刀上把攻击软化掉——这不是 prompt
写得不够狠，是那一层把手拉住了。A3（任务优先）没有那一层，你让它拆台它就把拆台
当成一个要做好的任务。

裁判用 `gpt-5.6-luna` + `kimi-k2.5`——**必须来自没参与生产的厂商**，否则等于
自己评自己。

---

## 计划中 · 尚未实现

以下是讨论过但**代码里还没有**的东西，不要当成现状：

- **`advised` 档位**：在两个必停点插入两个旗舰模型（网页订阅，非 API）作为顾问，
  人看完两份建议后做综合判断。目的是降低人的决策成本，同时把「人到底加了什么」
  变成可测量的东西——记录人的决定相对于两个顾问是否偏离
- **多行输入**：现在 `console.input()` 只读一行，粘贴多行会被切碎喂给后续提示。
  2026-09-04 那次真实运行就栽在这上面
- **P3 阶段**：链条到 2D-fix 就结束，方法论的 P3（文档展开）整个没做
- **灰线回退点**：方法论允许「回 P1.0 重选角色」和「P3 → P2」，两个都没实现。
  引擎里唯一的流程级回退是 2D-fix → 回 P1
