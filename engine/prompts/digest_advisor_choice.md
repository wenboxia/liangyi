下面是一份建议正文。写它的人被要求从几个选项里选一个，并说明理由。

请判断：**这份建议实际主张选哪个选项？**

## 选项

{options}

## 硬性要求

- 只看它**实际主张**什么，不看它嘴上说了哪个数字。有人会说"我选 2"然后论证的
  其实是 3 的内容，以实际论证为准
- 判断不了就填 `unknown`，不要猜
- 不评价这份建议对不对，只归类

## 输出格式

只输出 JSON，不加解释或代码块标记：

{"选项": "1", "依据": "一句话说你为什么这么归类"}

「选项」填选项编号，或 `unknown`。

## 建议正文

{advice}
